<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>积极向上好青年-博客</title>
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://zhangchaozc.cn/"/>
  <updated>2019-01-01T14:53:39.751Z</updated>
  <id>http://zhangchaozc.cn/</id>
  
  <author>
    <name>积极向上好青年</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>SparkSQL</title>
    <link href="http://zhangchaozc.cn/2019/01/01/hadoop/SparkSQL/"/>
    <id>http://zhangchaozc.cn/2019/01/01/hadoop/SparkSQL/</id>
    <published>2019-01-01T14:50:41.000Z</published>
    <updated>2019-01-01T14:53:39.751Z</updated>
    
    <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>Spark SQL踩坑</p>
<p>SparkSQL就是将大量的数据加载到内存中（本身这些数据都是规则的，相当于MySQL数据库中的表一样），然后用SQL语句进行查询，查询的结果也可以再导入MySQL</p>
<h3 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h3><p>安装MySQL，我最开始是在Windows上安装的MySQL，Windows和Linux虚拟机能ping通，无奈，程序跑起来Linux上没有权限访问Windows上的数据库，试了很久没搞定，于是就在Linux上装MySQL，和Spark程序在一台节点上</p>
<p>准备一些数据<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line">1 小明 22</div><div class="line">2 小红 21</div><div class="line">3 小张 25</div><div class="line">4 小王 19</div><div class="line">5 小陈 17</div><div class="line">6 zc 18</div><div class="line">7 pang 39</div><div class="line">8 yuan 28</div><div class="line">9 小文 10</div><div class="line">10 liu 12</div></pre></td></tr></table></figure></p>
<p>有规则的数据，上传数据到HDFS上</p>
<a id="more"></a>
<h3 id="code"><a href="#code" class="headerlink" title="code"></a>code</h3><figure class="highlight scala"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">import</span> java.util.<span class="type">Properties</span></div><div class="line"><span class="keyword">import</span> org.apache.spark.rdd.<span class="type">RDD</span></div><div class="line"><span class="keyword">import</span> org.apache.spark.sql.&#123;<span class="type">DataFrame</span>, <span class="type">SparkSession</span>&#125;</div><div class="line"><span class="comment">//创建样例类Student</span></div><div class="line"><span class="keyword">case</span> <span class="class"><span class="keyword">class</span> <span class="title">Student</span>(<span class="params">id: <span class="type">Int</span>, name: <span class="type">String</span>, age: <span class="type">Int</span></span>)</span></div><div class="line"></div><div class="line"><span class="class"><span class="keyword">object</span> <span class="title">SparkSqlToMysql</span> </span>&#123;</div><div class="line">  <span class="function"><span class="keyword">def</span> <span class="title">main</span></span>(args: <span class="type">Array</span>[<span class="type">String</span>]): <span class="type">Unit</span> = &#123;</div><div class="line">    <span class="comment">//1、创建sparkSession对象</span></div><div class="line">    <span class="keyword">val</span> spark: <span class="type">SparkSession</span> = <span class="type">SparkSession</span>.builder()</div><div class="line">      .appName(<span class="string">"SparkSqlToMysql"</span>)</div><div class="line">      .getOrCreate()</div><div class="line">    <span class="comment">//2、读取数据</span></div><div class="line">    <span class="keyword">val</span> data: <span class="type">RDD</span>[<span class="type">String</span>] = spark.sparkContext.textFile(<span class="string">"hdfs://hadoop01:9000/student"</span>)</div><div class="line">    <span class="comment">//3、切分每一行,</span></div><div class="line">    <span class="keyword">val</span> arrRDD: <span class="type">RDD</span>[<span class="type">Array</span>[<span class="type">String</span>]] = data.map(_.split(<span class="string">" "</span>))</div><div class="line">    <span class="comment">//4、RDD关联Student</span></div><div class="line">    <span class="keyword">val</span> studentRDD: <span class="type">RDD</span>[<span class="type">Student</span>] = arrRDD.map(x =&gt; <span class="type">Student</span>(x(<span class="number">0</span>).toInt, x(<span class="number">1</span>), x(<span class="number">2</span>).toInt))</div><div class="line">    <span class="comment">//导入隐式转换</span></div><div class="line">    <span class="keyword">import</span> spark.implicits._</div><div class="line">    <span class="comment">//5、将RDD转换成DataFrame</span></div><div class="line">    <span class="keyword">val</span> studentDF: <span class="type">DataFrame</span> = studentRDD.toDF()</div><div class="line">    <span class="comment">//6、将DataFrame注册成表</span></div><div class="line">    studentDF.createOrReplaceTempView(<span class="string">"student"</span>)</div><div class="line">    <span class="comment">//7、操作student表 ,按照年龄进行降序排列</span></div><div class="line">    <span class="keyword">val</span> resultDF: <span class="type">DataFrame</span> = spark.sql(<span class="string">"select * from student order by age desc"</span>)</div><div class="line">    <span class="comment">//8、把结果保存在mysql表中</span></div><div class="line">    <span class="comment">//创建Properties对象，配置连接mysql的用户名和密码</span></div><div class="line">    <span class="keyword">val</span> prop = <span class="keyword">new</span> <span class="type">Properties</span>()</div><div class="line">    prop.setProperty(<span class="string">"user"</span>, <span class="string">"root"</span>)</div><div class="line">    prop.setProperty(<span class="string">"password"</span>, <span class="string">"123456"</span>)</div><div class="line">    resultDF.write.jdbc(<span class="string">"jdbc:mysql://hadoop01:3306/spark?useUnicode=true&amp;characterEncoding=UTF-8"</span>, <span class="string">"student"</span>, prop)</div><div class="line">    <span class="comment">//写入mysql时，可以配置插入mode，overwrite覆盖，append追加，ignore忽略，error默认表存在报错</span></div><div class="line">    <span class="comment">//resultDF.write.mode(SaveMode.Overwrite).jdbc("jdbc:mysql://192.168.200.150:3306/spark","student",prop)</span></div><div class="line">    spark.stop()</div><div class="line">  &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>函数textFile的功能特别强大，文件路径支持通配符，比如你要一次性导入文件夹，或者多个文件只需要<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">textFile(/xxx/*/*)</div></pre></td></tr></table></figure></p>
<p>就可以全部导入RDD中</p>
<h3 id="打包运行踩坑"><a href="#打包运行踩坑" class="headerlink" title="打包运行踩坑"></a>打包运行踩坑</h3><p>运行，指定MySQL连接驱动包<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">./spark-submit --class demo03.SparkSqlToMysql --executor-memory 500m --jars /root/Desktop/mysql-connector-java-5.1.38.jar --driver-class-path /root/Desktop/mysql-connector-java-5.1.38.jar /root/Desktop/scalaDemo.jar</div></pre></td></tr></table></figure></p>
<p>也可以运行spark-shell<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">./spark-shell --executor-memory 500m --jars /root/Desktop/mysql-connector-java-5.1.38.jar --driver-class-path /root/Desktop/mysql-connector-java-5.1.38.jar</div></pre></td></tr></table></figure></p>
<p>通过shell直接操作</p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h3&gt;&lt;p&gt;Spark SQL踩坑&lt;/p&gt;
&lt;p&gt;SparkSQL就是将大量的数据加载到内存中（本身这些数据都是规则的，相当于MySQL数据库中的表一样），然后用SQL语句进行查询，查询的结果也可以再导入MySQL&lt;/p&gt;
&lt;h3 id=&quot;准备工作&quot;&gt;&lt;a href=&quot;#准备工作&quot; class=&quot;headerlink&quot; title=&quot;准备工作&quot;&gt;&lt;/a&gt;准备工作&lt;/h3&gt;&lt;p&gt;安装MySQL，我最开始是在Windows上安装的MySQL，Windows和Linux虚拟机能ping通，无奈，程序跑起来Linux上没有权限访问Windows上的数据库，试了很久没搞定，于是就在Linux上装MySQL，和Spark程序在一台节点上&lt;/p&gt;
&lt;p&gt;准备一些数据&lt;br&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;3&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;4&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;5&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;6&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;7&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;8&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;9&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;10&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1 小明 22&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2 小红 21&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;3 小张 25&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;4 小王 19&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;5 小陈 17&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;6 zc 18&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;7 pang 39&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;8 yuan 28&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;9 小文 10&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;10 liu 12&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
&lt;p&gt;有规则的数据，上传数据到HDFS上&lt;/p&gt;
    
    </summary>
    
      <category term="hadoop" scheme="http://zhangchaozc.cn/categories/hadoop/"/>
    
    
      <category term="大数据" scheme="http://zhangchaozc.cn/tags/%E5%A4%A7%E6%95%B0%E6%8D%AE/"/>
    
  </entry>
  
  <entry>
    <title>Spark入门</title>
    <link href="http://zhangchaozc.cn/2019/01/01/hadoop/Spark%E5%85%A5%E9%97%A8/"/>
    <id>http://zhangchaozc.cn/2019/01/01/hadoop/Spark入门/</id>
    <published>2019-01-01T14:50:15.000Z</published>
    <updated>2019-01-01T14:52:54.714Z</updated>
    
    <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>Spark入门学习</p>
<h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><h4 id="安装scala"><a href="#安装scala" class="headerlink" title="安装scala"></a>安装scala</h4><p>下载<code>scala-2.12.7.tgz</code>，解压&amp;&amp;环境变量</p>
<h4 id="安装Spark"><a href="#安装Spark" class="headerlink" title="安装Spark"></a>安装Spark</h4><p>下载<code>spark-2.4.0-bin-hadoop2.7.tgz</code>，解压&amp;&amp;环境变量<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"># scala</div><div class="line">export SCALA_HOME=/usr/scala</div><div class="line">export PATH=$PATH:$SCALA_HOME/bin</div><div class="line"># spark</div><div class="line">export SPARK_HOME=/hadoop/spark-2.4.0</div><div class="line">export PATH=$PATH:$SPARK_HOME/bin</div></pre></td></tr></table></figure></p>
<h5 id="spark-env-sh"><a href="#spark-env-sh" class="headerlink" title="spark-env.sh"></a>spark-env.sh</h5><p>先重命名<code>spark-env.sh.template</code>，vim增加如下内容<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line">JAVA_HOME=/usr/java/jdk1.8.0_171</div><div class="line">SCALA_HOME=/usr/scala</div><div class="line">HADOOP_HOME=/hadoop-2.8.4</div><div class="line">HADOOP_CONF_DIR=/hadoop/hadoop-2.8.4/etc/hadoop</div><div class="line">SPARK_MASTER_IP=hadoop01</div><div class="line">SPARK_MASTER_PORT=7077</div><div class="line">SPARK_MASTER_WEBUI_PORT=8080</div><div class="line">SPARK_WORKER_CORES=1</div><div class="line">SPARK_WORKER_MEMORY=2g</div><div class="line">SPARK_WORKER_PORT=7078</div><div class="line">SPARK_WORKER_WEBUI_PORT=8081</div><div class="line">SPARK_WORKER_INSTANCES=1</div></pre></td></tr></table></figure></p>
<a id="more"></a>
<h5 id="spark-defaults-conf"><a href="#spark-defaults-conf" class="headerlink" title="spark-defaults.conf"></a>spark-defaults.conf</h5><p><code>mv spark-defaults.conf.template spark-defaults.conf</code><br><code>vim spark-defaults.conf</code><br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">spark.master                     spark://hadoop01:7077</div></pre></td></tr></table></figure></p>
<h5 id="slaves"><a href="#slaves" class="headerlink" title="slaves"></a>slaves</h5><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">mv slaves.template slaves</div><div class="line">vim slaves</div></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">hadoop02</div></pre></td></tr></table></figure>
<h5 id="拷贝"><a href="#拷贝" class="headerlink" title="拷贝"></a>拷贝</h5><p>在hadoop02上安装scala并且将hadoop01上的Spark目录拷贝到hadoop02<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">安装，环境变量配置略...</div><div class="line">scp -r /hadoop/spark-2.4.0/ hadoop02:/hadoop/</div></pre></td></tr></table></figure></p>
<h5 id="启动"><a href="#启动" class="headerlink" title="启动"></a>启动</h5><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">/sbin/start-all.sh</div></pre></td></tr></table></figure>
<p><code>jps</code>查看进程，发现多了Master节点和Worker节点，说明安装成功~，管理界面：<a href="http://hadoop01:8080/" target="_blank" rel="external">http://hadoop01:8080/</a></p>
<h3 id="WordCount程序"><a href="#WordCount程序" class="headerlink" title="WordCount程序"></a>WordCount程序</h3><p>Windows上下载scala环境，IDEA下载scala插件，新建一个maven项目，将scala插件添加到项目中</p>
<h4 id="pom"><a href="#pom" class="headerlink" title="pom"></a>pom</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div><div class="line">43</div><div class="line">44</div><div class="line">45</div></pre></td><td class="code"><pre><div class="line">&lt;properties&gt;</div><div class="line">    &lt;project.build.sourceEncoding&gt;UTF-8&lt;/project.build.sourceEncoding&gt;</div><div class="line">    &lt;scala.version&gt;2.11&lt;/scala.version&gt;</div><div class="line">    &lt;spark.version&gt;2.4.0&lt;/spark.version&gt;</div><div class="line">&lt;/properties&gt;</div><div class="line"></div><div class="line">&lt;dependencies&gt;</div><div class="line">    &lt;dependency&gt;</div><div class="line">        &lt;groupId&gt;org.apache.spark&lt;/groupId&gt;</div><div class="line">        &lt;artifactId&gt;spark-core_$&#123;scala.version&#125;&lt;/artifactId&gt;</div><div class="line">        &lt;version&gt;$&#123;spark.version&#125;&lt;/version&gt;</div><div class="line">    &lt;/dependency&gt;</div><div class="line">    &lt;dependency&gt;</div><div class="line">        &lt;groupId&gt;org.apache.spark&lt;/groupId&gt;</div><div class="line">        &lt;artifactId&gt;spark-sql_$&#123;scala.version&#125;&lt;/artifactId&gt;</div><div class="line">        &lt;version&gt;$&#123;spark.version&#125;&lt;/version&gt;</div><div class="line">    &lt;/dependency&gt;</div><div class="line">    &lt;dependency&gt;</div><div class="line">        &lt;groupId&gt;org.apache.spark&lt;/groupId&gt;</div><div class="line">        &lt;artifactId&gt;spark-streaming_$&#123;scala.version&#125;&lt;/artifactId&gt;</div><div class="line">        &lt;version&gt;$&#123;spark.version&#125;&lt;/version&gt;</div><div class="line">    &lt;/dependency&gt;</div><div class="line">&lt;/dependencies&gt;</div><div class="line"></div><div class="line">&lt;build&gt;</div><div class="line">    &lt;finalName&gt;scalaDemo&lt;/finalName&gt;</div><div class="line">    &lt;plugins&gt;</div><div class="line">        &lt;plugin&gt;</div><div class="line">            &lt;!-- 这是个编译scala代码的 --&gt;</div><div class="line">            &lt;groupId&gt;net.alchim31.maven&lt;/groupId&gt;</div><div class="line">            &lt;artifactId&gt;scala-maven-plugin&lt;/artifactId&gt;</div><div class="line">            &lt;version&gt;3.2.1&lt;/version&gt;</div><div class="line">            &lt;executions&gt;</div><div class="line">                &lt;execution&gt;</div><div class="line">                    &lt;id&gt;scala-compile-first&lt;/id&gt;</div><div class="line">                    &lt;phase&gt;process-resources&lt;/phase&gt;</div><div class="line">                    &lt;goals&gt;</div><div class="line">                        &lt;goal&gt;add-source&lt;/goal&gt;</div><div class="line">                        &lt;goal&gt;compile&lt;/goal&gt;</div><div class="line">                    &lt;/goals&gt;</div><div class="line">                &lt;/execution&gt;</div><div class="line">            &lt;/executions&gt;</div><div class="line">        &lt;/plugin&gt;</div><div class="line">    &lt;/plugins&gt;</div><div class="line">&lt;/build&gt;</div></pre></td></tr></table></figure>
<p>注意plugin标签上一定要添加scala的插件，否则直接点maven-&gt;compile时，scala文件无法编译成class文件</p>
<h5 id="code"><a href="#code" class="headerlink" title="code"></a>code</h5><figure class="highlight scala"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="class"><span class="keyword">object</span> <span class="title">WordCount</span> </span>&#123;</div><div class="line">  <span class="function"><span class="keyword">def</span> <span class="title">main</span></span>(args: <span class="type">Array</span>[<span class="type">String</span>]): <span class="type">Unit</span> = &#123;</div><div class="line">    <span class="keyword">val</span> conf = <span class="keyword">new</span> <span class="type">SparkConf</span>().setAppName(<span class="string">"WC"</span>)</div><div class="line">    <span class="keyword">val</span> sc = <span class="keyword">new</span> <span class="type">SparkContext</span>(conf)</div><div class="line">    <span class="keyword">val</span> line = sc.textFile(<span class="string">"hdfs://hadoop01:9000/wc_test_02"</span>)</div><div class="line">    <span class="keyword">val</span> result = line.flatMap(_.split(<span class="string">" "</span>)).map((_, <span class="number">1</span>)).reduceByKey(_ + _)</div><div class="line">    result.saveAsTextFile(<span class="string">"hdfs://hadoop01:9000/spark/out2"</span>)</div><div class="line">    sc.stop()</div><div class="line">  &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>然后编译，打包，上传到linux，准备运行。。</p>
<h3 id="踩坑错误"><a href="#踩坑错误" class="headerlink" title="踩坑错误"></a>踩坑错误</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">Initial job has not accepted any resources; check your cluster UI to ensure that workers are registered and have sufficient resources</div></pre></td></tr></table></figure>
<p>搞了好几个小时，Spark太吃内存了吧，解决办法：</p>
<p>修改spark-env.sh<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">SPARK_WORKER_MEMORY=800M</div></pre></td></tr></table></figure></p>
<p>把内存搞小一点，然后运行<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">./spark-submit --class demo02.WordCount --executor-memory 500m /root/Desktop/scalaDemo.jar</div></pre></td></tr></table></figure></p>
<p>指定的内存要小于800m，然后就运行成功了，也没一点提示（不报错就是成功）。</p>
<p>结果<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">(brother,1)</div><div class="line">(hello,4)</div><div class="line">(world,1)</div><div class="line">(spark,1)</div><div class="line">(hadoop,1)</div></pre></td></tr></table></figure></p>
<p>一个简单的Spark WordCount程序，比写MapReduce简单很多。</p>
<p>中途遇到问题，SparkSubmit进程为什么杀不死？kill也不行，最后进程太多，只能重启虚拟机来解决了</p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h3&gt;&lt;p&gt;Spark入门学习&lt;/p&gt;
&lt;h3 id=&quot;安装&quot;&gt;&lt;a href=&quot;#安装&quot; class=&quot;headerlink&quot; title=&quot;安装&quot;&gt;&lt;/a&gt;安装&lt;/h3&gt;&lt;h4 id=&quot;安装scala&quot;&gt;&lt;a href=&quot;#安装scala&quot; class=&quot;headerlink&quot; title=&quot;安装scala&quot;&gt;&lt;/a&gt;安装scala&lt;/h4&gt;&lt;p&gt;下载&lt;code&gt;scala-2.12.7.tgz&lt;/code&gt;，解压&amp;amp;&amp;amp;环境变量&lt;/p&gt;
&lt;h4 id=&quot;安装Spark&quot;&gt;&lt;a href=&quot;#安装Spark&quot; class=&quot;headerlink&quot; title=&quot;安装Spark&quot;&gt;&lt;/a&gt;安装Spark&lt;/h4&gt;&lt;p&gt;下载&lt;code&gt;spark-2.4.0-bin-hadoop2.7.tgz&lt;/code&gt;，解压&amp;amp;&amp;amp;环境变量&lt;br&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;3&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;4&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;5&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;6&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;# scala&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;export SCALA_HOME=/usr/scala&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;export PATH=$PATH:$SCALA_HOME/bin&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;# spark&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;export SPARK_HOME=/hadoop/spark-2.4.0&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;export PATH=$PATH:$SPARK_HOME/bin&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
&lt;h5 id=&quot;spark-env-sh&quot;&gt;&lt;a href=&quot;#spark-env-sh&quot; class=&quot;headerlink&quot; title=&quot;spark-env.sh&quot;&gt;&lt;/a&gt;spark-env.sh&lt;/h5&gt;&lt;p&gt;先重命名&lt;code&gt;spark-env.sh.template&lt;/code&gt;，vim增加如下内容&lt;br&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;3&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;4&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;5&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;6&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;7&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;8&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;9&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;10&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;11&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;12&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;JAVA_HOME=/usr/java/jdk1.8.0_171&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;SCALA_HOME=/usr/scala&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;HADOOP_HOME=/hadoop-2.8.4&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;HADOOP_CONF_DIR=/hadoop/hadoop-2.8.4/etc/hadoop&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;SPARK_MASTER_IP=hadoop01&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;SPARK_MASTER_PORT=7077&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;SPARK_MASTER_WEBUI_PORT=8080&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;SPARK_WORKER_CORES=1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;SPARK_WORKER_MEMORY=2g&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;SPARK_WORKER_PORT=7078&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;SPARK_WORKER_WEBUI_PORT=8081&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;SPARK_WORKER_INSTANCES=1&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="hadoop" scheme="http://zhangchaozc.cn/categories/hadoop/"/>
    
    
      <category term="大数据" scheme="http://zhangchaozc.cn/tags/%E5%A4%A7%E6%95%B0%E6%8D%AE/"/>
    
  </entry>
  
  <entry>
    <title>HBase</title>
    <link href="http://zhangchaozc.cn/2018/11/03/hadoop/HBase/"/>
    <id>http://zhangchaozc.cn/2018/11/03/hadoop/HBase/</id>
    <published>2018-11-03T02:48:06.000Z</published>
    <updated>2018-11-03T02:57:42.650Z</updated>
    
    <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>学习HBase</p>
<ul>
<li>HBase基础知识</li>
<li>HBase分布式搭建</li>
<li>HBase Shell</li>
<li>HBase的批量导入</li>
<li>HBase的Java API</li>
</ul>
<p>简介<br>高可靠、高性能、面向列、可伸缩的分布式存储系统。HBase利用HDFS作为底层的文件系统，利用MapReduce来处理数据，Zookeeper为协调工具</p>
<p>逻辑视图如下图所示：</p>
<p><img src="/images/hadoop_010.PNG" alt="hadoop_010"></p>
<p>数据模型，行</p>
<ul>
<li>每行有可排序关键字和任意列</li>
<li>字符串、二进制串都可以作为行键</li>
<li>表按照行键有序处理</li>
<li>可以只对一行上锁，对行的写操作是原子的</li>
</ul>
<p>数据模型，列</p>
<ul>
<li>列族，定义为“族：标签”</li>
<li>数据通过时间戳分版本</li>
<li>列族在建表后不能改变，但是一个列族中列标签可以任意增加</li>
</ul>
<a id="more"></a>
<p>系统总体结构图</p>
<p><img src="/images/hadoop_011.PNG" alt="hadoop_011"></p>
<ul>
<li>主从结构</li>
<li>可以有多个Master，但Zookeeper的选举机制保证有一个Master运行</li>
<li>HBase建立在HDFS之上</li>
</ul>
<h3 id="安装HBase"><a href="#安装HBase" class="headerlink" title="安装HBase"></a>安装HBase</h3><p>下载，<a href="http://mirrors.hust.edu.cn/apache/hbase/2.1.1/hbase-2.1.1-bin.tar.gz" target="_blank" rel="external">http://mirrors.hust.edu.cn/apache/hbase/2.1.1/hbase-2.1.1-bin.tar.gz</a></p>
<p>复制到虚拟机，解压安装包<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">tar -zxvf hbase-2.1.1-bin.tar.gz</div></pre></td></tr></table></figure></p>
<h4 id="配置环境变量"><a href="#配置环境变量" class="headerlink" title="配置环境变量"></a>配置环境变量</h4><p><code>vim /etc/profile</code><br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"># hbase</div><div class="line">export HBASE_HOME=/hadoop/hbase-2.1.1</div><div class="line">export PATH=$PATH:$HBASE_HOME/bin</div></pre></td></tr></table></figure></p>
<p><code>source /etc/profile</code></p>
<h4 id="vim-hbase-env-sh"><a href="#vim-hbase-env-sh" class="headerlink" title="vim hbase-env.sh"></a>vim hbase-env.sh</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">export JAVA_HOME=/usr/java/jdk1.8.0_171</div><div class="line"># 设置由hbase自己管理zookeeper，不需要安装单独的zookeeper</div><div class="line">export HBASE_MANAGES_ZK=true</div></pre></td></tr></table></figure>
<h4 id="vim-hbase-site-xml"><a href="#vim-hbase-site-xml" class="headerlink" title="vim hbase-site.xml"></a>vim hbase-site.xml</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div></pre></td><td class="code"><pre><div class="line">&lt;configuration&gt;</div><div class="line">     &lt;property&gt;</div><div class="line">          &lt;name&gt;hbase.rootdir&lt;/name&gt;</div><div class="line">          &lt;value&gt;hdfs://hadoop01:9000/hbase&lt;/value&gt;</div><div class="line">     &lt;/property&gt;</div><div class="line">	 &lt;property&gt;</div><div class="line">        &lt;name&gt;hbase.cluster.distributed&lt;/name&gt;</div><div class="line">        &lt;value&gt;true&lt;/value&gt;</div><div class="line">    &lt;/property&gt;</div><div class="line">    &lt;property&gt;</div><div class="line">        &lt;name&gt;hbase.master&lt;/name&gt;</div><div class="line">        &lt;value&gt;hadoop01&lt;/value&gt;</div><div class="line">    &lt;/property&gt;</div><div class="line">    &lt;property&gt;</div><div class="line">        &lt;name&gt;hbase.zookeeper.quorum&lt;/name&gt;</div><div class="line">        &lt;value&gt;hadoop01,hadoop02&lt;/value&gt;</div><div class="line">    &lt;/property&gt;</div><div class="line">	&lt;property&gt;</div><div class="line">        &lt;name&gt;dfs.replication&lt;/name&gt;</div><div class="line">        &lt;value&gt;1&lt;/value&gt;</div><div class="line">    &lt;/property&gt;</div><div class="line">&lt;/configuration&gt;</div></pre></td></tr></table></figure>
<h4 id="vim-regionservers"><a href="#vim-regionservers" class="headerlink" title="vim regionservers"></a>vim regionservers</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">hadoop02</div></pre></td></tr></table></figure>
<h4 id="拷贝已经配置好的机器"><a href="#拷贝已经配置好的机器" class="headerlink" title="拷贝已经配置好的机器"></a>拷贝已经配置好的机器</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">scp -r /hadoop/hbase-2.1.1 hadoop02:/hadoop/</div></pre></td></tr></table></figure>
<p>拷贝前可以删除HBASE_HOME下的docs目录</p>
<p>拷贝完后在hadoop02上配置一下环境变量</p>
<h4 id="启动HBase"><a href="#启动HBase" class="headerlink" title="启动HBase"></a>启动HBase</h4><p>进入bin目录下<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">./start-hbase.sh</div></pre></td></tr></table></figure></p>
<p>启动完成，查看HDFS下是否有hbase目录，发现没有，启动失败</p>
<h4 id="查看错误"><a href="#查看错误" class="headerlink" title="查看错误"></a>查看错误</h4><p>进入logs目录，查看文件<code>hbase-root-master-hadoop01</code>，发现错误<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">java.lang.NoClassDefFoundError: org/apache/htrace/SamplerBuilder</div><div class="line">	at org.apache.hadoop.hdfs.DFSClient.&lt;init&gt;(DFSClient.java:644)</div><div class="line">	at org.apache.hadoop.hdfs.DFSClient.&lt;init&gt;(DFSClient.java:628)</div><div class="line">	at org.apache.hadoop.hdfs.DistributedFileSystem.initialize(DistributedFileSystem.java:149)</div><div class="line">	at org.apache.hadoop.fs.FileSystem.createFileSystem(FileSystem.java:2667)</div></pre></td></tr></table></figure></p>
<h4 id="解决"><a href="#解决" class="headerlink" title="解决"></a>解决</h4><p>百度了一下，拷贝一下jar包<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">cp /hadoop/hbase-2.1.1/lib/client-facing-thirdparty/htrace-core-3.1.0-incubating.jar /hadoop/hbase-2.1.1/lib/</div></pre></td></tr></table></figure></p>
<p>两个节点都执行这个命令，重新启动，OK，hbase目录有了</p>
<h4 id="查看进程"><a href="#查看进程" class="headerlink" title="查看进程"></a>查看进程</h4><p>master<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">7072 ResourceManager</div><div class="line">16817 HRegionServer</div><div class="line">6837 SecondaryNameNode</div><div class="line">6358 NameNode</div><div class="line">30118 Jps</div><div class="line">25755 HQuorumPeer</div><div class="line">28187 HMaster</div><div class="line">30015 HBaseConfTool</div></pre></td></tr></table></figure></p>
<p>slave<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">7314 NodeManager</div><div class="line">22612 HQuorumPeer</div><div class="line">16362 HRegionServer</div><div class="line">6858 DataNode</div><div class="line">26079 Jps</div></pre></td></tr></table></figure></p>
<p>一切正常</p>
<h3 id="Hbase-Shell"><a href="#Hbase-Shell" class="headerlink" title="Hbase Shell"></a>Hbase Shell</h3><p>提供了基本的增删改查，如下图所示：</p>
<p><img src="/images/hadoop_012.PNG" alt="hadoop_012"></p>
<h4 id="基本增删改查"><a href="#基本增删改查" class="headerlink" title="基本增删改查"></a>基本增删改查</h4><p>创建表，列出所有的表<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line">hbase(main):002:0&gt; create &apos;user&apos;,&apos;user_id&apos;,&apos;address&apos;,&apos;info&apos;</div><div class="line">Created table user</div><div class="line">Took 25.6470 seconds                                                                                                                                         </div><div class="line">=&gt; Hbase::Table - user</div><div class="line">hbase(main):003:0&gt; list</div><div class="line">TABLE                                                                                                                                                        </div><div class="line">user                                                                                                                                                         </div><div class="line">1 row(s)</div><div class="line">Took 0.2597 seconds                                                                                                                                          </div><div class="line">=&gt; [&quot;user&quot;]</div></pre></td></tr></table></figure></p>
<p>得到表的信息<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">describe &apos;user&apos;</div></pre></td></tr></table></figure></p>
<p>删除表<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">disable &apos;user&apos;，或者</div><div class="line">drop &apos;user&apos;</div></pre></td></tr></table></figure></p>
<p>添加信息，在一个列族上可以无限扩展<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">hbase(main):005:0&gt; put &apos;user&apos;,&apos;xiaoming&apos;,&apos;info:age&apos;,&apos;24&apos;</div><div class="line">Took 2.7663 seconds                                                                                                                                          </div><div class="line">hbase(main):006:0&gt; put &apos;user&apos;,&apos;xiaoming&apos;,&apos;info:company&apos;,&apos;alibaba&apos;</div><div class="line">Took 0.0777 seconds</div></pre></td></tr></table></figure></p>
<p>获取一条记录<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">//获取一个ID下所有数据</div><div class="line">get &apos;user&apos;,&apos;xiaoming&apos;</div><div class="line">//获取一个列族</div><div class="line">get &apos;user&apos;,&apos;xiaoming&apos;,&apos;info&apos;</div><div class="line">//获取一个列</div><div class="line">get &apos;user&apos;,&apos;xiaoming&apos;,&apos;info:age&apos;</div></pre></td></tr></table></figure></p>
<p>更新记录，和put一个记录一样<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">put &apos;user&apos;,&apos;xiaoming&apos;,&apos;info:age&apos; ,&apos;29&apos;</div><div class="line">get &apos;user&apos;,&apos;xiaoming&apos;,&apos;info:age&apos;</div><div class="line">put &apos;user&apos;,&apos;xiaoming&apos;,&apos;info:age&apos; ,&apos;30&apos;</div><div class="line">get &apos;user&apos;,&apos;xiaoming&apos;,&apos;info:age&apos;</div></pre></td></tr></table></figure></p>
<p>被覆盖的数据也能查到<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">get &apos;user&apos;,&apos;xiaoming&apos;,&#123;COLUMN=&gt;&apos;info:age&apos;,VERSIONS=&gt;1&#125;</div><div class="line">get &apos;user&apos;,&apos;xiaoming&apos;,&#123;COLUMN=&gt;&apos;info:age&apos;,VERSIONS=&gt;2&#125;</div><div class="line">get &apos;user&apos;,&apos;xiaoming&apos;,&#123;COLUMN=&gt;&apos;info:age&apos;,VERSIONS=&gt;3&#125;</div></pre></td></tr></table></figure></p>
<p>获取某个时间戳下的数据<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">get &apos;user&apos;,&apos;xiaoming&apos;,&#123;COLUMN=&gt;&apos;info:age&apos;,TIMESTAMP=&gt;1364874937056&#125;</div></pre></td></tr></table></figure></p>
<p>全表扫描<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">scan &apos;user&apos;</div></pre></td></tr></table></figure></p>
<p>删除列<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">delete &apos;user&apos;,&apos;xiaoming&apos;,&apos;info:age&apos;</div><div class="line">get &apos;user&apos;,&apos;xiaoming&apos;</div></pre></td></tr></table></figure></p>
<p>删除整行<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">deleteall &apos;user&apos;,&apos;xiaoming&apos;</div></pre></td></tr></table></figure></p>
<p>统计表的行数<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">count &apos;user&apos;</div></pre></td></tr></table></figure></p>
<p>清空表<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">truncate &apos;user&apos;</div></pre></td></tr></table></figure></p>
<h4 id="Java-API"><a href="#Java-API" class="headerlink" title="Java API"></a>Java API</h4><p>导入hbase-client包，函数有点过时了</p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h3&gt;&lt;p&gt;学习HBase&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;HBase基础知识&lt;/li&gt;
&lt;li&gt;HBase分布式搭建&lt;/li&gt;
&lt;li&gt;HBase Shell&lt;/li&gt;
&lt;li&gt;HBase的批量导入&lt;/li&gt;
&lt;li&gt;HBase的Java API&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;简介&lt;br&gt;高可靠、高性能、面向列、可伸缩的分布式存储系统。HBase利用HDFS作为底层的文件系统，利用MapReduce来处理数据，Zookeeper为协调工具&lt;/p&gt;
&lt;p&gt;逻辑视图如下图所示：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/images/hadoop_010.PNG&quot; alt=&quot;hadoop_010&quot;&gt;&lt;/p&gt;
&lt;p&gt;数据模型，行&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;每行有可排序关键字和任意列&lt;/li&gt;
&lt;li&gt;字符串、二进制串都可以作为行键&lt;/li&gt;
&lt;li&gt;表按照行键有序处理&lt;/li&gt;
&lt;li&gt;可以只对一行上锁，对行的写操作是原子的&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;数据模型，列&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;列族，定义为“族：标签”&lt;/li&gt;
&lt;li&gt;数据通过时间戳分版本&lt;/li&gt;
&lt;li&gt;列族在建表后不能改变，但是一个列族中列标签可以任意增加&lt;/li&gt;
&lt;/ul&gt;
    
    </summary>
    
      <category term="hadoop" scheme="http://zhangchaozc.cn/categories/hadoop/"/>
    
    
      <category term="大数据" scheme="http://zhangchaozc.cn/tags/%E5%A4%A7%E6%95%B0%E6%8D%AE/"/>
    
  </entry>
  
  <entry>
    <title>hadoop之MapReduce</title>
    <link href="http://zhangchaozc.cn/2018/10/28/hadoop/hadoop%E4%B9%8BMapReduce/"/>
    <id>http://zhangchaozc.cn/2018/10/28/hadoop/hadoop之MapReduce/</id>
    <published>2018-10-28T15:30:08.000Z</published>
    <updated>2018-10-28T15:32:04.508Z</updated>
    
    <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>学习MapReduce</p>
<h3 id="输入输出"><a href="#输入输出" class="headerlink" title="输入输出"></a>输入输出</h3><ul>
<li>map，输入（k1,v1&gt;），输出（k2,v2）</li>
<li>reduce，输入就是将map的输出按key值group起来，（k2,list(v2)），输出需要得到的结果（k3,v3）</li>
</ul>
<p>对于WordCound程序而言，例如文件<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">hello hadoop</div><div class="line">hello map</div><div class="line">hello reduce</div></pre></td></tr></table></figure></p>
<p>默认按行读数据，即每读入一行数据，调用一次map方法，map输入的key值意义不大，可以是行号，可以是字符偏移，默认是字符偏移，即map的输入：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">&lt;0,&quot;hello hadoop&quot;&gt;</div><div class="line">&lt;13,&quot;hello map&quot;&gt;</div><div class="line">&lt;23,&quot;hello reduce&quot;&gt;</div></pre></td></tr></table></figure></p>
<p>即要调用3次map函数，对于WC程序而言，就是统计每个单词出现的次数，那么map的输出就是<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">&lt;&quot;hello&quot;,1&gt;,&lt;&quot;hadoop&quot;,1&gt;</div><div class="line">&lt;&quot;hello&quot;,1&gt;,&lt;&quot;map&quot;,1&gt;</div><div class="line">&lt;&quot;hello&quot;,1&gt;,&lt;&quot;reduce&quot;,1&gt;</div></pre></td></tr></table></figure></p>
<p>reduce的输入就是将map输出的值相同的key值group成list，输入<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">&lt;&quot;hello&quot;,list(1,1,1)&gt;</div><div class="line">&lt;&quot;hadoop&quot;,list(1)&gt;</div><div class="line">&lt;&quot;map&quot;,list(1)&gt;</div><div class="line">&lt;&quot;reduce&quot;,list(1)&gt;</div></pre></td></tr></table></figure></p>
<p>输出的结果就是WC的计算结果，将list的数据累加就行</p>
<a id="more"></a>
<p>由此可见map和reduce的接口很抽象，实现的key,value全靠程序员</p>
<h3 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h3><p>Mapper接口，要实现这个接口<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line"><span class="comment">//泛型，map的输入、输出键值对</span></div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Mapper</span>&lt;<span class="title">KEYIN</span>, <span class="title">VALUEIN</span>, <span class="title">KEYOUT</span>, <span class="title">VALUEOUT</span>&gt; </span>&#123;</div><div class="line">    <span class="comment">//重写map方法</span></div><div class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">map</span><span class="params">(KEYIN key, VALUEIN value, Mapper&lt;KEYIN, VALUEIN, KEYOUT, VALUEOUT&gt;.Context context)</span> <span class="keyword">throws</span> IOException, InterruptedException </span>&#123;</div><div class="line">        <span class="comment">//context为内部类Context</span></div><div class="line">        context.write(key, value);</div><div class="line">    &#125;</div><div class="line">    <span class="comment">//重要，定义了一个内部类Context对象继承于MapContext</span></div><div class="line">    <span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">Context</span> <span class="keyword">implements</span> <span class="title">MapContext</span>&lt;<span class="title">KEYIN</span>, <span class="title">VALUEIN</span>, <span class="title">KEYOUT</span>, <span class="title">VALUEOUT</span>&gt; </span>&#123;</div><div class="line">        <span class="function"><span class="keyword">public</span> <span class="title">Context</span><span class="params">()</span> </span>&#123;</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    ......</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>Reducer接口，需要实现这个接口<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div></pre></td><td class="code"><pre><div class="line"><span class="comment">//输入输出</span></div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Reducer</span>&lt;<span class="title">KEYIN</span>, <span class="title">VALUEIN</span>, <span class="title">KEYOUT</span>, <span class="title">VALUEOUT</span>&gt; </span>&#123;</div><div class="line">    <span class="comment">//重写reduce方法</span></div><div class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">reduce</span><span class="params">(KEYIN key, Iterable&lt;VALUEIN&gt; values, Reducer&lt;KEYIN, VALUEIN, KEYOUT, VALUEOUT&gt;.Context context)</span> <span class="keyword">throws</span> IOException, InterruptedException </span>&#123;</div><div class="line">        Iterator i$ = values.iterator();</div><div class="line"></div><div class="line">        <span class="keyword">while</span>(i$.hasNext()) &#123;</div><div class="line">            VALUEIN value = i$.next();</div><div class="line">            context.write(key, value);</div><div class="line">        &#125;</div><div class="line"></div><div class="line">    &#125;</div><div class="line">    <span class="comment">//内部类Context继承于ReduceContext</span></div><div class="line">    <span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">Context</span> <span class="keyword">implements</span> <span class="title">ReduceContext</span>&lt;<span class="title">KEYIN</span>, <span class="title">VALUEIN</span>, <span class="title">KEYOUT</span>, <span class="title">VALUEOUT</span>&gt; </span>&#123;</div><div class="line">        <span class="function"><span class="keyword">public</span> <span class="title">Context</span><span class="params">()</span> </span>&#123;</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    ......</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h3 id="combine方法"><a href="#combine方法" class="headerlink" title="combine方法"></a>combine方法</h3><p>减少reduce的IO开销，将map产生的结果在本地先进行一个合并处理</p>
<p>对于WC程序而言，CombinerClass和ReducerClass一样<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">job.setMapperClass(TokenizerMapper.class);</div><div class="line">//CombinerClass和ReducerClass一样</div><div class="line">job.setCombinerClass(IntSumReducer.class);</div><div class="line">job.setReducerClass(IntSumReducer.class);</div></pre></td></tr></table></figure></p>
<h3 id="partition方法"><a href="#partition方法" class="headerlink" title="partition方法"></a>partition方法</h3><p>将reduce的输出结果分不同的文件存放，它的计算只依赖于key值</p>
<h3 id="MapReduce的输入输出"><a href="#MapReduce的输入输出" class="headerlink" title="MapReduce的输入输出"></a>MapReduce的输入输出</h3><ul>
<li>能处理很多格式的数据，text、数据库等</li>
<li>输入被分成InputSplit，每个Mapper处理一个InputSplit</li>
<li>每个InputSplit被分成多条记录record，每一个record产生一个key-value</li>
</ul>
<p>默认的输入处理类是TextInputFormat</p>
<ul>
<li>处理固定数量的记录，NLineIputFormat</li>
<li>处理二进制数据，BinaryInputFormat</li>
<li>多类不同类型的文件，MultipleInputs</li>
<li>数据库文件，DBInputFormat</li>
</ul>
<p>输出同理，和输入一样分这么多类型</p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h3&gt;&lt;p&gt;学习MapReduce&lt;/p&gt;
&lt;h3 id=&quot;输入输出&quot;&gt;&lt;a href=&quot;#输入输出&quot; class=&quot;headerlink&quot; title=&quot;输入输出&quot;&gt;&lt;/a&gt;输入输出&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;map，输入（k1,v1&amp;gt;），输出（k2,v2）&lt;/li&gt;
&lt;li&gt;reduce，输入就是将map的输出按key值group起来，（k2,list(v2)），输出需要得到的结果（k3,v3）&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;对于WordCound程序而言，例如文件&lt;br&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;3&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;hello hadoop&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;hello map&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;hello reduce&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
&lt;p&gt;默认按行读数据，即每读入一行数据，调用一次map方法，map输入的key值意义不大，可以是行号，可以是字符偏移，默认是字符偏移，即map的输入：&lt;br&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;3&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;&amp;lt;0,&amp;quot;hello hadoop&amp;quot;&amp;gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&amp;lt;13,&amp;quot;hello map&amp;quot;&amp;gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&amp;lt;23,&amp;quot;hello reduce&amp;quot;&amp;gt;&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
&lt;p&gt;即要调用3次map函数，对于WC程序而言，就是统计每个单词出现的次数，那么map的输出就是&lt;br&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;3&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;&amp;lt;&amp;quot;hello&amp;quot;,1&amp;gt;,&amp;lt;&amp;quot;hadoop&amp;quot;,1&amp;gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&amp;lt;&amp;quot;hello&amp;quot;,1&amp;gt;,&amp;lt;&amp;quot;map&amp;quot;,1&amp;gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&amp;lt;&amp;quot;hello&amp;quot;,1&amp;gt;,&amp;lt;&amp;quot;reduce&amp;quot;,1&amp;gt;&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
&lt;p&gt;reduce的输入就是将map输出的值相同的key值group成list，输入&lt;br&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;3&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;4&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;&amp;lt;&amp;quot;hello&amp;quot;,list(1,1,1)&amp;gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&amp;lt;&amp;quot;hadoop&amp;quot;,list(1)&amp;gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&amp;lt;&amp;quot;map&amp;quot;,list(1)&amp;gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&amp;lt;&amp;quot;reduce&amp;quot;,list(1)&amp;gt;&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
&lt;p&gt;输出的结果就是WC的计算结果，将list的数据累加就行&lt;/p&gt;
    
    </summary>
    
      <category term="hadoop" scheme="http://zhangchaozc.cn/categories/hadoop/"/>
    
    
      <category term="大数据" scheme="http://zhangchaozc.cn/tags/%E5%A4%A7%E6%95%B0%E6%8D%AE/"/>
    
  </entry>
  
  <entry>
    <title>hadoop之IO操作</title>
    <link href="http://zhangchaozc.cn/2018/10/28/hadoop/hadoop%E4%B9%8BIO%E6%93%8D%E4%BD%9C/"/>
    <id>http://zhangchaozc.cn/2018/10/28/hadoop/hadoop之IO操作/</id>
    <published>2018-10-28T15:29:42.000Z</published>
    <updated>2018-10-28T15:32:59.995Z</updated>
    
    <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>学习hadoop的IO</p>
<h3 id="main"><a href="#main" class="headerlink" title="main"></a>main</h3><ul>
<li>数据完整性检查</li>
<li>数据压缩</li>
<li>数据序列化</li>
<li>基于文件的数据结构</li>
</ul>
<h3 id="数据完整性"><a href="#数据完整性" class="headerlink" title="数据完整性"></a>数据完整性</h3><p>hadoop采用CRC-32校验和检查数据完整性</p>
<h4 id="本地文件系统的数据完整性"><a href="#本地文件系统的数据完整性" class="headerlink" title="本地文件系统的数据完整性"></a>本地文件系统的数据完整性</h4><p>在创建文件a的同时，会创建隐藏的文件a.crc记录了文件的校验和，默认每512个字节就产生32位的校验和</p>
<p>可以自定义配置core-site.xml<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">property</span>&gt;</span>　　　</div><div class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>io.bytes.per.checksum<span class="tag">&lt;/<span class="name">name</span>&gt;</span>　　</div><div class="line">    <span class="tag">&lt;<span class="name">value</span>&gt;</span>512<span class="tag">&lt;/<span class="name">value</span>&gt;</span>　　</div><div class="line"><span class="tag">&lt;/<span class="name">property</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p>也可以自定义是否启用校验和<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line">   <span class="tag">&lt;<span class="name">property</span>&gt;</span>　　　</div><div class="line">       <span class="tag">&lt;<span class="name">name</span>&gt;</span>fs.file.impl<span class="tag">&lt;/<span class="name">name</span>&gt;</span>　　</div><div class="line">       <span class="tag">&lt;<span class="name">value</span>&gt;</span>org.apache.hadoop.fs.LocalFileSystem<span class="tag">&lt;/<span class="name">value</span>&gt;</span>　　</div><div class="line">   <span class="tag">&lt;/<span class="name">property</span>&gt;</span></div><div class="line"><span class="comment">&lt;!--上面启用校验和，下面禁用--&gt;</span></div><div class="line">   <span class="tag">&lt;<span class="name">property</span>&gt;</span>　　　</div><div class="line">       <span class="tag">&lt;<span class="name">name</span>&gt;</span>fs.file.impl<span class="tag">&lt;/<span class="name">name</span>&gt;</span>　　</div><div class="line">       <span class="tag">&lt;<span class="name">value</span>&gt;</span>org.apache.hadoop.fs.RawLocalFileSystem<span class="tag">&lt;/<span class="name">value</span>&gt;</span>　　</div><div class="line">   <span class="tag">&lt;/<span class="name">property</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p>任何在配置文件中修改的配置都可以通过Java代码在Configuration类中局部的配置</p>
<a id="more"></a>
<h4 id="HDFS的数据完整性"><a href="#HDFS的数据完整性" class="headerlink" title="HDFS的数据完整性"></a>HDFS的数据完整性</h4><p>HDFS使用ChecksumFileSystem类来校验</p>
<p>RawLocalFileSystem和ChecksumFileSystem一起使用，可以达到LocalFileSystem的效果</p>
<p>Java中可以通过函数setVerifyChecksum(false)来局部的禁止校验</p>
<h3 id="数据压缩"><a href="#数据压缩" class="headerlink" title="数据压缩"></a>数据压缩</h3><p>hadoop支持的压缩算法</p>
<p><img src="/images/hadoop_007.PNG" alt="hadoop_007"></p>
<p>其中LZO不在hadoop的发布版本中，需要单独下载</p>
<ul>
<li>利用CompressionCodec压缩和解压流</li>
<li>利用CompressionCodecFactory推断压缩算法</li>
</ul>
<p>在MapReduce中使用压缩</p>
<ul>
<li>如果输入数据被压缩，通过使用CompressionCodecFactory自动推断Codec对象，MapReduce程序会自动将输入数据解压</li>
<li>如果希望MapReduce输出压缩文件，设置属性<code>mapred.output.compress</code>为<code>true</code>，将<code>mapred.output.compression.codec</code>属性设置为要使用的Codec类</li>
</ul>
<h3 id="IO序列化（重要）"><a href="#IO序列化（重要）" class="headerlink" title="IO序列化（重要）"></a>IO序列化（重要）</h3><ul>
<li>序列化是将内存中的对象转化为字节流，反序列化则是将字节流恢复到内存中</li>
<li>系列化的目的，1是进程间的通信，2是数据的持久化</li>
<li>hadoop利用RPC实现进程间的通信</li>
<li>由于Java本身序列化的弊端，hadoop实现了一套自己的序列化机制</li>
</ul>
<p>序列化的核心，Writable接口<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Writable</span> </span>&#123;</div><div class="line">    <span class="comment">//将对象序列化到输出流，DataOutput、DataInput均为二进制流</span></div><div class="line">    <span class="function"><span class="keyword">void</span> <span class="title">write</span><span class="params">(DataOutput out)</span> <span class="keyword">throws</span> IOException</span>;</div><div class="line">    <span class="comment">//输入流反序列化</span></div><div class="line">    <span class="function"><span class="keyword">void</span> <span class="title">readFields</span><span class="params">(DataInput in)</span> <span class="keyword">throws</span> IOException</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>接口的继承关系图</p>
<p><img src="/images/hadoop_008.PNG" alt="hadoop_008"></p>
<p>由图可以看出，hadoop的基本数据类型都是继承于WritableComparable接口，即这种类型既是可序列化的，又是可比较大小的。而其它的存储数据结构ArrayWritable和MapWritable只继承于Writable接口</p>
<p>Comparable和Comparator<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Comparable</span>&lt;<span class="title">T</span>&gt;</span>&#123;</div><div class="line">    <span class="comment">//隐含了一个this指针，this和o对象比较</span></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compareTo</span><span class="params">(T o)</span></span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Comparator</span>&lt;<span class="title">T</span>&gt;</span>&#123;</div><div class="line">    <span class="comment">//是一个工具类，实现两个对象的比较</span></div><div class="line">    <span class="function"><span class="keyword">int</span> <span class="title">compare</span><span class="params">(T o1,T o2)</span></span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>直接在字节流层面上进行比较<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">RawComparator</span>&lt;<span class="title">T</span>&gt; <span class="keyword">extends</span> <span class="title">Comparator</span>&lt;<span class="title">T</span>&gt; </span>&#123;</div><div class="line">    <span class="function"><span class="keyword">int</span> <span class="title">compare</span><span class="params">(<span class="keyword">byte</span>[] b1, <span class="keyword">int</span> s1, <span class="keyword">int</span> l1, <span class="keyword">byte</span>[] b2, <span class="keyword">int</span> s2, <span class="keyword">int</span> l2)</span></span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h4 id="hadoop数据类型"><a href="#hadoop数据类型" class="headerlink" title="hadoop数据类型"></a>hadoop数据类型</h4><p>hadoop数据类型就是对Java数据类型的封装，如下图所示：</p>
<p><img src="/images/hadoop_009.PNG" alt="hadoop_009"></p>
<p>自定义数据类型，实现WritableComparable接口<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TextPair</span> <span class="keyword">implements</span> <span class="title">WritableComparable</span>&lt;<span class="title">TextPair</span>&gt; </span>&#123;</div><div class="line">    <span class="keyword">private</span> Text first;</div><div class="line">    <span class="keyword">private</span> Text second;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="title">TextPair</span><span class="params">(Text f, Text s)</span> </span>&#123;</div><div class="line">        first = f;</div><div class="line">        second = s;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compareTo</span><span class="params">(TextPair o)</span> </span>&#123;</div><div class="line">        <span class="keyword">int</span> cmp = first.compareTo(o.first);</div><div class="line">        <span class="keyword">if</span> (cmp != <span class="number">0</span>) &#123;</div><div class="line">            <span class="keyword">return</span> cmp;</div><div class="line">        &#125;</div><div class="line">        <span class="keyword">return</span> second.compareTo(o.second);</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">write</span><span class="params">(DataOutput dataOutput)</span> <span class="keyword">throws</span> IOException </span>&#123;</div><div class="line">        first.write(dataOutput);</div><div class="line">        second.write(dataOutput);</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">readFields</span><span class="params">(DataInput dataInput)</span> <span class="keyword">throws</span> IOException </span>&#123;</div><div class="line">        first.readFields(dataInput);</div><div class="line">        first.readFields(dataInput);</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h4 id="序列化框架"><a href="#序列化框架" class="headerlink" title="序列化框架"></a>序列化框架</h4><ul>
<li>hadoop支持不同的序列化框架，可以在配置文件中配置</li>
<li>实现序列化框架需要实现Serialization接口</li>
<li>默认的序列框架是org.apache.hadoop.io.serializer.WritableSerialization</li>
</ul>
<h4 id="基于文件的数据接口"><a href="#基于文件的数据接口" class="headerlink" title="基于文件的数据接口"></a>基于文件的数据接口</h4><ul>
<li>解决hadoop中小文件太多浪费资源的问题，SequenceFile</li>
<li>小文件的文件名为key，文件内容为value写入SequenceFile文件</li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h3&gt;&lt;p&gt;学习hadoop的IO&lt;/p&gt;
&lt;h3 id=&quot;main&quot;&gt;&lt;a href=&quot;#main&quot; class=&quot;headerlink&quot; title=&quot;main&quot;&gt;&lt;/a&gt;main&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;数据完整性检查&lt;/li&gt;
&lt;li&gt;数据压缩&lt;/li&gt;
&lt;li&gt;数据序列化&lt;/li&gt;
&lt;li&gt;基于文件的数据结构&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;数据完整性&quot;&gt;&lt;a href=&quot;#数据完整性&quot; class=&quot;headerlink&quot; title=&quot;数据完整性&quot;&gt;&lt;/a&gt;数据完整性&lt;/h3&gt;&lt;p&gt;hadoop采用CRC-32校验和检查数据完整性&lt;/p&gt;
&lt;h4 id=&quot;本地文件系统的数据完整性&quot;&gt;&lt;a href=&quot;#本地文件系统的数据完整性&quot; class=&quot;headerlink&quot; title=&quot;本地文件系统的数据完整性&quot;&gt;&lt;/a&gt;本地文件系统的数据完整性&lt;/h4&gt;&lt;p&gt;在创建文件a的同时，会创建隐藏的文件a.crc记录了文件的校验和，默认每512个字节就产生32位的校验和&lt;/p&gt;
&lt;p&gt;可以自定义配置core-site.xml&lt;br&gt;&lt;figure class=&quot;highlight xml&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;3&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;4&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;&lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;property&lt;/span&gt;&amp;gt;&lt;/span&gt;　　　&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;    &lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;name&lt;/span&gt;&amp;gt;&lt;/span&gt;io.bytes.per.checksum&lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;name&lt;/span&gt;&amp;gt;&lt;/span&gt;　　&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;    &lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;value&lt;/span&gt;&amp;gt;&lt;/span&gt;512&lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;value&lt;/span&gt;&amp;gt;&lt;/span&gt;　　&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;property&lt;/span&gt;&amp;gt;&lt;/span&gt;&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
&lt;p&gt;也可以自定义是否启用校验和&lt;br&gt;&lt;figure class=&quot;highlight xml&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;3&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;4&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;5&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;6&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;7&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;8&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;9&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;   &lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;property&lt;/span&gt;&amp;gt;&lt;/span&gt;　　　&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;       &lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;name&lt;/span&gt;&amp;gt;&lt;/span&gt;fs.file.impl&lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;name&lt;/span&gt;&amp;gt;&lt;/span&gt;　　&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;       &lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;value&lt;/span&gt;&amp;gt;&lt;/span&gt;org.apache.hadoop.fs.LocalFileSystem&lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;value&lt;/span&gt;&amp;gt;&lt;/span&gt;　　&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;   &lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;property&lt;/span&gt;&amp;gt;&lt;/span&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;span class=&quot;comment&quot;&gt;&amp;lt;!--上面启用校验和，下面禁用--&amp;gt;&lt;/span&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;   &lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;property&lt;/span&gt;&amp;gt;&lt;/span&gt;　　　&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;       &lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;name&lt;/span&gt;&amp;gt;&lt;/span&gt;fs.file.impl&lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;name&lt;/span&gt;&amp;gt;&lt;/span&gt;　　&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;       &lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;value&lt;/span&gt;&amp;gt;&lt;/span&gt;org.apache.hadoop.fs.RawLocalFileSystem&lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;value&lt;/span&gt;&amp;gt;&lt;/span&gt;　　&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;   &lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;property&lt;/span&gt;&amp;gt;&lt;/span&gt;&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
&lt;p&gt;任何在配置文件中修改的配置都可以通过Java代码在Configuration类中局部的配置&lt;/p&gt;
    
    </summary>
    
      <category term="hadoop" scheme="http://zhangchaozc.cn/categories/hadoop/"/>
    
    
      <category term="大数据" scheme="http://zhangchaozc.cn/tags/%E5%A4%A7%E6%95%B0%E6%8D%AE/"/>
    
  </entry>
  
  <entry>
    <title>hadoop之HDFS</title>
    <link href="http://zhangchaozc.cn/2018/10/27/hadoop/hadoop%E4%B9%8BHDFS/"/>
    <id>http://zhangchaozc.cn/2018/10/27/hadoop/hadoop之HDFS/</id>
    <published>2018-10-27T14:02:14.000Z</published>
    <updated>2018-10-27T14:09:52.839Z</updated>
    
    <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>学习HDFS文件系统</p>
<h3 id="分布式文件系统"><a href="#分布式文件系统" class="headerlink" title="分布式文件系统"></a>分布式文件系统</h3><ul>
<li>数据量很多，一台机器放不下，需要多台机器共同存储</li>
<li>允许文件通过网络在多台主机上分享</li>
<li>通透性，对于用户来说，使用存储和普通单机没什么区别</li>
<li>容错，某些机器挂了，对系统没有影响</li>
</ul>
<h3 id="hadoop文件系统"><a href="#hadoop文件系统" class="headerlink" title="hadoop文件系统"></a>hadoop文件系统</h3><ul>
<li>对Linux文件系统的抽象。抽象类为<code>org.apache.hadoop.fs.FileSystem</code>，HDFS只是一个实例</li>
<li>丰富的Shell命令</li>
<li>提供JAVA API编程接口</li>
</ul>
<p><img src="/images/hadoop_004.PNG" alt="hadoop_004"></p>
<a id="more"></a>
<h3 id="HDFS"><a href="#HDFS" class="headerlink" title="HDFS"></a>HDFS</h3><ul>
<li>主从结构，通过心跳机制维持</li>
<li>NameNode是master，DataNode是slave</li>
<li>处理超大文件</li>
<li>机器不要很好的性能</li>
<li>不适合处理小文件</li>
<li>不适合任意修改文件</li>
</ul>
<h3 id="命令行（整体设计与Linux命令行相似）"><a href="#命令行（整体设计与Linux命令行相似）" class="headerlink" title="命令行（整体设计与Linux命令行相似）"></a>命令行（整体设计与Linux命令行相似）</h3><p>列出所有文件<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">hadoop fs -ls /</div></pre></td></tr></table></figure></p>
<p>本地文件上传到hdfs<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">hadoop fs -copyFromLocal /root/xxx hdfs:/hadoop01:9000/xxx</div><div class="line">hadoop fs -put /root/xxx hdfs:/hadoop01:9000/xxx</div></pre></td></tr></table></figure></p>
<p>下载文件，是否加校验和<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">hadoop fs -copyToLocal [-ignorecrc] [-crc] /root/xxx hdfs:/hadoop01:9000/xxx</div><div class="line">hadoop fs -get [-ignorecrc] [-crc] /root/xxx hdfs:/hadoop01:9000/xxx</div></pre></td></tr></table></figure></p>
<p>删除文件，文件夹多加<code>-r</code>参数<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">hadoop fs -rm hdfs://hadoop01:9000/wc_test_01</div></pre></td></tr></table></figure></p>
<h3 id="Java-API"><a href="#Java-API" class="headerlink" title="Java API"></a>Java API</h3><ul>
<li>Configuration类，封装了所有的配置</li>
<li>FileSystem类，是个抽象类，DistributedFileSystem是它的实现之一</li>
<li>FSDataInputStream和FSDataOutputStream是HDFS的输入和输出流，分别通过FileSystem的open方法和create方法获得</li>
</ul>
<h3 id="如何从HDFS读数据？"><a href="#如何从HDFS读数据？" class="headerlink" title="如何从HDFS读数据？"></a>如何从HDFS读数据？</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FileSystemCat</span> </span>&#123;</div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</div><div class="line">        String url = <span class="string">"hdfs://hadoop01:9000/wc_test_01"</span>;</div><div class="line">        Configuration conf = <span class="keyword">new</span> Configuration();</div><div class="line">        FileSystem fs = FileSystem.get(URI.create(url), conf);</div><div class="line">        InputStream in = <span class="keyword">null</span>;</div><div class="line">        <span class="keyword">try</span> &#123;</div><div class="line">            in = fs.open(<span class="keyword">new</span> Path(url));</div><div class="line">            IOUtils.copyBytes(in, System.out, <span class="number">4</span> * <span class="number">1024</span>, <span class="keyword">false</span>);</div><div class="line">        &#125; <span class="keyword">finally</span> &#123;</div><div class="line">            IOUtils.closeStream(in);</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>文件内容将打印在控制台上</p>
<h3 id="如何从HDFS写数据？"><a href="#如何从HDFS写数据？" class="headerlink" title="如何从HDFS写数据？"></a>如何从HDFS写数据？</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FileCopy</span> </span>&#123;</div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception </span>&#123;</div><div class="line">        String local = <span class="string">"D:/1.txt"</span>;</div><div class="line">        String dst = <span class="string">"hdfs://hadoop01:9000/1.txt"</span>;</div><div class="line"><span class="comment">//        String local = args[0];</span></div><div class="line"><span class="comment">//        String dst = args[1];</span></div><div class="line">        InputStream in = <span class="keyword">new</span> BufferedInputStream(<span class="keyword">new</span> FileInputStream(local));</div><div class="line">        Configuration conf = <span class="keyword">new</span> Configuration();</div><div class="line">        FileSystem fs = FileSystem.get(URI.create(dst), conf, <span class="string">"root"</span>);</div><div class="line">        OutputStream out = fs.create(<span class="keyword">new</span> Path(dst));</div><div class="line">        IOUtils.copyBytes(in, out, <span class="number">4</span> * <span class="number">1024</span>, <span class="keyword">true</span>);</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>不能直接上传Windows的文件，权限不够；但可以伪装成root用户，这样就可以上传</p>
<h3 id="如何创建目录"><a href="#如何创建目录" class="headerlink" title="如何创建目录"></a>如何创建目录</h3><p>这个方法会创建不存在的父目录<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">public boolen mkdirs(Path f) throws IOException</div></pre></td></tr></table></figure></p>
<h3 id="如何查询文件系统"><a href="#如何查询文件系统" class="headerlink" title="如何查询文件系统"></a>如何查询文件系统</h3><p>查询元数据和目录结构<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">FileStatus  getFileStatus( Path f) throws IOException</div></pre></td></tr></table></figure></p>
<p>FileSystem的listStatus提供了列出文件目录内容的方法<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">FileStatus[]  listStatus( Path f) throws IOException</div><div class="line">FileStatus[]  listStatus( Path f, PathFilter filter) throws IOException</div><div class="line">FileStatus[]  listStatus( Path[] files) throws IOException </div><div class="line">FileStatus[]  listStatus( Path[] files, PathFilter filter) throws IOException</div></pre></td></tr></table></figure></p>
<p>显示文件元数据信息<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ShowFileStatus</span> </span>&#123;</div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception </span>&#123;</div><div class="line">        String url = <span class="string">"hdfs://hadoop01:9000/wc_test_01"</span>;</div><div class="line">        Configuration conf = <span class="keyword">new</span> Configuration();</div><div class="line">        FileSystem fs = FileSystem.get(<span class="keyword">new</span> URI(url), conf);</div><div class="line">        Path path = <span class="keyword">new</span> Path(url);</div><div class="line">        FileStatus stat = fs.getFileStatus(path);</div><div class="line">        System.out.println(<span class="string">"Full path:"</span> + stat.getPath().toUri().getPath());</div><div class="line">        <span class="keyword">if</span> (stat.isDirectory()) &#123;</div><div class="line">            System.out.println(<span class="string">"is dir"</span>);</div><div class="line">        &#125; <span class="keyword">else</span> &#123;</div><div class="line">            System.out.println(<span class="string">"is file"</span>);</div><div class="line">        &#125;</div><div class="line">        System.out.println(<span class="string">"Length:"</span> + stat.getLen());</div><div class="line">        System.out.println(<span class="string">"Last modified time:"</span> + stat.getModificationTime());</div><div class="line">        System.out.println(<span class="string">"Replication:"</span> + stat.getReplication());</div><div class="line">        System.out.println(<span class="string">"Block size:"</span> + stat.getBlockSize());</div><div class="line">        System.out.println(<span class="string">"Owner:"</span> + stat.getOwner());</div><div class="line">        System.out.println(<span class="string">"Group:"</span> + stat.getGroup());</div><div class="line">        System.out.println(<span class="string">"Permission:"</span> + stat.getPermission());</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>列出目录内容<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ListStatus</span> </span>&#123;</div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception </span>&#123;</div><div class="line">        String url = <span class="string">"hdfs://hadoop01:9000/tmp"</span>;</div><div class="line">        Configuration conf = <span class="keyword">new</span> Configuration();</div><div class="line">        FileSystem fs = FileSystem.get(<span class="keyword">new</span> URI(url), conf, <span class="string">"root"</span>);</div><div class="line">        Path path = <span class="keyword">new</span> Path(url);</div><div class="line">        FileStatus[] statuses = fs.listStatus(path);</div><div class="line">        Path[] paths = FileUtil.stat2Paths(statuses);</div><div class="line">        <span class="keyword">for</span> (Path p : paths) &#123;</div><div class="line">            System.out.println(p);</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h3 id="利用通配符来实现文件的筛选"><a href="#利用通配符来实现文件的筛选" class="headerlink" title="利用通配符来实现文件的筛选"></a>利用通配符来实现文件的筛选</h3><p>利用<code>*</code>或者<code>?</code>来匹配文件<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">FileStatus[]  globStatus( Path pathPattern) throws IOException</div><div class="line">FileStatus[]  globStatus( Path pathPattern, PathFilter filter) throws IOException</div></pre></td></tr></table></figure></p>
<h3 id="利用正则式来筛选文件"><a href="#利用正则式来筛选文件" class="headerlink" title="利用正则式来筛选文件"></a>利用正则式来筛选文件</h3><p>实现接口<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">PathFilter</span></span>&#123;</div><div class="line">	<span class="function"><span class="keyword">boolean</span> <span class="title">accept</span><span class="params">(Path path)</span></span>; <span class="comment">//如果接受该path，则返回true</span></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h3 id="HDFS写文件原理"><a href="#HDFS写文件原理" class="headerlink" title="HDFS写文件原理"></a>HDFS写文件原理</h3><p>原理图如下</p>
<p><img src="/images/hadoop_005.PNG" alt="hadoop_005"></p>
<h3 id="HDFS读文件原理"><a href="#HDFS读文件原理" class="headerlink" title="HDFS读文件原理"></a>HDFS读文件原理</h3><p>原理图如下</p>
<p><img src="/images/hadoop_006.PNG" alt="hadoop_006"></p>
<p>原理过程先放着，以后再看..</p>
<h3 id="网络距离"><a href="#网络距离" class="headerlink" title="网络距离"></a>网络距离</h3><p>就是两个节点的物理距离。当文件需要拷贝时，距离近的节点优先，以提高性能</p>
<h3 id="HDFS一致性模型"><a href="#HDFS一致性模型" class="headerlink" title="HDFS一致性模型"></a>HDFS一致性模型</h3><p>HDFS提供了一个同步函数，这个方法是FSDataOutputStream类的sync()函数。当sync()函数返回成功时，HDFS就可以保证此时写入的文件数据文件是一致的并且对于所有用户是可见的。关闭一个文件时也隐式执行了sync()函数</p>
<h3 id="HDFS文件系统结构"><a href="#HDFS文件系统结构" class="headerlink" title="HDFS文件系统结构"></a>HDFS文件系统结构</h3><h3 id="HDFS文件镜像和编辑日志"><a href="#HDFS文件镜像和编辑日志" class="headerlink" title="HDFS文件镜像和编辑日志"></a>HDFS文件镜像和编辑日志</h3><ul>
<li>通常情况下，辅助namenode每隔一个小时(通过fs.checkpoint.period属性设置，以秒为单位)创建检查点</li>
<li>当编辑日志大小达到64MB(由fs.checkpoint.size属性设置，以字节为单位)创建检查点。系统每隔五分钟检查一次编辑日志大小</li>
</ul>
<p>用于误操作的恢复，和文件系统的备份</p>
<h3 id="归档文件（Archive）"><a href="#归档文件（Archive）" class="headerlink" title="归档文件（Archive）"></a>归档文件（Archive）</h3><p>HDFS不利于小文件的存储，浪费性能。可以将小文件打包成大文件存储<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">hadoop archive -archiveName files.har 1.txt 2.txt 3.txt</div></pre></td></tr></table></figure></p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h3&gt;&lt;p&gt;学习HDFS文件系统&lt;/p&gt;
&lt;h3 id=&quot;分布式文件系统&quot;&gt;&lt;a href=&quot;#分布式文件系统&quot; class=&quot;headerlink&quot; title=&quot;分布式文件系统&quot;&gt;&lt;/a&gt;分布式文件系统&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;数据量很多，一台机器放不下，需要多台机器共同存储&lt;/li&gt;
&lt;li&gt;允许文件通过网络在多台主机上分享&lt;/li&gt;
&lt;li&gt;通透性，对于用户来说，使用存储和普通单机没什么区别&lt;/li&gt;
&lt;li&gt;容错，某些机器挂了，对系统没有影响&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;hadoop文件系统&quot;&gt;&lt;a href=&quot;#hadoop文件系统&quot; class=&quot;headerlink&quot; title=&quot;hadoop文件系统&quot;&gt;&lt;/a&gt;hadoop文件系统&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;对Linux文件系统的抽象。抽象类为&lt;code&gt;org.apache.hadoop.fs.FileSystem&lt;/code&gt;，HDFS只是一个实例&lt;/li&gt;
&lt;li&gt;丰富的Shell命令&lt;/li&gt;
&lt;li&gt;提供JAVA API编程接口&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;img src=&quot;/images/hadoop_004.PNG&quot; alt=&quot;hadoop_004&quot;&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="hadoop" scheme="http://zhangchaozc.cn/categories/hadoop/"/>
    
    
      <category term="大数据" scheme="http://zhangchaozc.cn/tags/%E5%A4%A7%E6%95%B0%E6%8D%AE/"/>
    
  </entry>
  
  <entry>
    <title>搭建集群hadoop</title>
    <link href="http://zhangchaozc.cn/2018/10/16/hadoop/%E6%90%AD%E5%BB%BA%E9%9B%86%E7%BE%A4hadoop/"/>
    <id>http://zhangchaozc.cn/2018/10/16/hadoop/搭建集群hadoop/</id>
    <published>2018-10-16T08:19:14.000Z</published>
    <updated>2018-10-16T08:30:03.487Z</updated>
    
    <content type="html"><![CDATA[<h3 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h3><p>在单机的hadoop的基础上搭建集群的hadoop</p>
<p>先克隆3台虚拟机：<br>在VMware中，右键虚拟机（已关闭状态），依次选择<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">[管理]-&gt;[克隆]-&gt;[下一步]-&gt;[虚拟机中当前状态]-&gt;[下一步]</div></pre></td></tr></table></figure></p>
<p>如图所示</p>
<div align="center"><br><img src="/images/hadoop_001.PNG" alt="hadoop_001"><br></div>

<p>选择位置后克隆机器，克隆2~3台虚拟机即可</p>
<h3 id="修改网卡IP主机名"><a href="#修改网卡IP主机名" class="headerlink" title="修改网卡IP主机名"></a>修改网卡IP主机名</h3><p>克隆的机器叫做02，先右键机器-&gt;设置-&gt;网络适配器-&gt;高级，给02生成一个新的mac地址</p>
<div align="center"><br><img src="/images/hadoop_002.PNG" alt="hadoop_002"><br></div>

<p>更改规则<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">vim /etc/udev/rules.d/70-persistent-net.rules</div></pre></td></tr></table></figure></p>
<div align="center"><br><img src="/images/hadoop_003.PNG" alt="hadoop_003"><br></div>

<p>删除多余的网卡，将第二个网卡的NAME改为eth0</p>
<p>配置IP地址<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">vim /etc/sysconfig/network-scripts/ifcfg-eth0</div></pre></td></tr></table></figure></p>
<p>将IP地址修改为和01机器在同一个网段下，保证01和02相互能ping通<br>重启网络<code>service network restart</code>，测试一下ping，02与Windows和01都能互相ping通</p>
<p>和搭建伪分布式一样，修改主机名和hosts文件，修改完成<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">vim /etc/sysconfig/network</div><div class="line">vim /etc/hosts</div></pre></td></tr></table></figure></p>
<a id="more"></a>
<h3 id="删除原有的hadoop环境"><a href="#删除原有的hadoop环境" class="headerlink" title="删除原有的hadoop环境"></a>删除原有的hadoop环境</h3><p>我是复制了3份，VMware中共有01,02,03,04虚拟机，依次删除原有的hadoop（也可以先删除再拷贝的）<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"># 从环境变量中删除</div><div class="line">vim /etc/profile</div><div class="line"># 从磁盘中删除</div><div class="line">rm -rf /hadoop</div></pre></td></tr></table></figure></p>
<p>为了操作方便，可以用xshell工具连接，免得虚拟机来回切换</p>
<p>02,03,04删除root下的<code>.ssh</code>文件夹，重新生成秘钥<code>ssh-keygen -t rsa -C &quot;zhang0chao0@gmail.com&quot;</code></p>
<h3 id="开始安装"><a href="#开始安装" class="headerlink" title="开始安装"></a>开始安装</h3><p>将安装包<code>hadoop-2.8.4.tar.gz</code>上传到01机器上，和单机一样，解压并配置环境变量</p>
<p>hosts文件配置，01~04机器都追加hosts文件<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">192.168.8.2 hadoop01</div><div class="line">192.168.8.3 hadoop02</div><div class="line">192.168.8.4 hadoop03</div><div class="line">192.168.8.5 hadoop04</div></pre></td></tr></table></figure></p>
<p>cd到目录<code>/hadoop/hadoop-2.8.4/etc/hadoop</code>，开始修改配置文件</p>
<p>修改JAVA_HOME<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">vim hadoop-env.sh</div></pre></td></tr></table></figure></p>
<p>core-site.xml，配置namenode和tmp目录<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line">&lt;configuration&gt;</div><div class="line">　　&lt;property&gt;</div><div class="line">　　　　&lt;name&gt;fs.default.name&lt;/name&gt;</div><div class="line">　　　　&lt;value&gt;hdfs://192.168.8.2:9000&lt;/value&gt;</div><div class="line">　　&lt;/property&gt;</div><div class="line">　　&lt;property&gt;</div><div class="line">　　　　&lt;name&gt;hadoop.tmp.dir&lt;/name&gt;</div><div class="line">　　　　&lt;value&gt;/hadoop/hadoop-2.8.4/tmp&lt;/value&gt;</div><div class="line">　　&lt;/property&gt;</div><div class="line">&lt;/configuration&gt;</div></pre></td></tr></table></figure></p>
<p>hdfs-site.xml，配置副本数量和name/data-node本地文件位置<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line">&lt;configuration&gt;</div><div class="line">　　&lt;property&gt;</div><div class="line">　　　　&lt;name&gt;dfs.replication&lt;/name&gt;</div><div class="line">　　　　&lt;value&gt;3&lt;/value&gt;</div><div class="line">　　&lt;/property&gt;</div><div class="line">　　&lt;property&gt;</div><div class="line">　　　　&lt;name&gt;dfs.namenode.name.dir&lt;/name&gt;</div><div class="line">　　　　&lt;value&gt;/hadoop/hadoop-2.8.4/tmp/dfs/name&lt;/value&gt;</div><div class="line">　　&lt;/property&gt;</div><div class="line">　　&lt;property&gt;</div><div class="line">　　　　&lt;name&gt;dfs.datanode.data.dir&lt;/name&gt;</div><div class="line">　　　　&lt;value&gt;/hadoop/hadoop-2.8.4/tmp/dfs/data&lt;/value&gt;</div><div class="line">　　&lt;/property&gt;</div><div class="line">&lt;/configuration&gt;</div></pre></td></tr></table></figure></p>
<p>mapred-site.xml，指定运行在YARN上<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">&lt;configuration&gt;</div><div class="line">　　&lt;property&gt;</div><div class="line">　　　　&lt;name&gt;mapreduce.framework.name&lt;/name&gt;</div><div class="line">　　　　&lt;value&gt;yarn&lt;/value&gt;</div><div class="line">　　&lt;/property&gt;</div><div class="line">&lt;/configuration&gt;</div></pre></td></tr></table></figure></p>
<p>yarn-site.xml，指定resourcemanager的地址，可以不和namenode在一台机器上，以减轻压力<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line">&lt;configuration&gt;</div><div class="line">　　&lt;property&gt;</div><div class="line">　　　　&lt;name&gt;yarn.resourcemanager.hostname&lt;/name&gt;</div><div class="line">　　　　&lt;value&gt;hadoop02&lt;/value&gt;</div><div class="line">　　&lt;/property&gt;</div><div class="line">　　&lt;property&gt;</div><div class="line">　　　　&lt;name&gt;yarn.nodemanager.aux-services&lt;/name&gt;</div><div class="line">　　　　&lt;value&gt;mapreduce_shuffle&lt;/value&gt;</div><div class="line">　　&lt;/property&gt;</div><div class="line">&lt;/configuration&gt;</div></pre></td></tr></table></figure></p>
<p>slaves，添加slave节点<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">hadoop02</div><div class="line">hadoop03</div><div class="line">hadoop04</div></pre></td></tr></table></figure></p>
<p>将配置好的文件拷贝到其它机器，在拷贝前先删除doc文档，提高拷贝的速度<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">rm -rf /hadoop/hadoop-2.8.4/share/doc/</div></pre></td></tr></table></figure></p>
<p>拷贝<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">scp -r /hadoop/ hadoop02:/</div><div class="line">scp -r /hadoop/ hadoop03:/</div><div class="line">scp -r /hadoop/ hadoop04:/</div></pre></td></tr></table></figure></p>
<p>02,03,04机器上添加环境变量<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">vim /etc/profile</div><div class="line">export HADOOP_HOME=/hadoop/hadoop-2.8.4</div><div class="line">export PATH=$PATH:$HADOOP_HOME/bin</div><div class="line">source /etc/profile</div></pre></td></tr></table></figure></p>
<p>01机器上格式化hdfs<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">hadoop namenode -format</div></pre></td></tr></table></figure></p>
<h3 id="启动hdfs"><a href="#启动hdfs" class="headerlink" title="启动hdfs"></a>启动hdfs</h3><p>cd到sbin目录，命令<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">./start-dfs.sh</div></pre></td></tr></table></figure></p>
<p>然后启动了namenode后，在启动datanode的时候卡住了，卡着不动，怎么办？找错误</p>
<p>在启动hadoop03机器的datanode时卡出了，就去对应的日志文件找，找到日志<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">vim hadoop-root-datanode-hadoop03.log</div></pre></td></tr></table></figure></p>
<p>发现错误<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">2018-10-14 05:34:44,052 WARN org.apache.hadoop.ipc.Client: Failed to connect to server: hadoop01/192.168.8.2:9000: retries get failed due to exceeded maximum allowed retries number: 10</div><div class="line">java.net.ConnectException: Connection refused</div><div class="line">	at sun.nio.ch.SocketChannelImpl.checkConnect(Native Method)</div><div class="line">	at sun.nio.ch.SocketChannelImpl.finishConnect(SocketChannelImpl.java:717)</div><div class="line">	xxxxxx</div></pre></td></tr></table></figure></p>
<p>就是hadoop03无法与hadoop01通信，那么就配置一下秘钥</p>
<h4 id="配ssh"><a href="#配ssh" class="headerlink" title="配ssh"></a>配ssh</h4><p>从01-&gt;[02,03,04]<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">ssh-copy-id hadoop02</div><div class="line">ssh-copy-id hadoop03</div><div class="line">ssh-copy-id hadoop04</div></pre></td></tr></table></figure></p>
<p>从[02,03,04]-&gt;01<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">ssh-copy-id hadoop01</div></pre></td></tr></table></figure></p>
<p>再次启动hdfs，成功~</p>
<h3 id="启动yarn"><a href="#启动yarn" class="headerlink" title="启动yarn"></a>启动yarn</h3><p>因为我在hadoop02上配置的resourcemanager，那么就在hadoop02上启动yarn</p>
<p>和01一样，将02与[01,03,04]之间都配上ssh权限，启动<code>sbin/start-yarn.sh</code>，出现错误<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">The authenticity of host &apos;hadoop02 (192.168.8.3)&apos; can&apos;t be established.</div></pre></td></tr></table></figure></p>
<p>自己到自己没有免登陆，再<code>ssh-copy-id localhost</code>，再次启动。终于成功</p>
<h3 id="检验进程"><a href="#检验进程" class="headerlink" title="检验进程"></a>检验进程</h3><p>挨个<code>jps</code>，发现hadoop02<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">2595 DataNode</div><div class="line">3653 Jps</div><div class="line">3480 NodeManager</div><div class="line">2815 ResourceManager</div></pre></td></tr></table></figure></p>
<p>ResourceManager和NodeManager在一起，而hadoop01缺少NodeManager</p>
<p>解决，编辑hadoop02中的slaves文件，修改为<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">hadoop01</div><div class="line">hadoop03</div><div class="line">hadoop04</div></pre></td></tr></table></figure></p>
<p>停止yarn后再运行，一切进程都正常</p>
<p>访问<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">http://192.168.8.2:50070/dfshealth.html#tab-overview</div><div class="line">http://192.168.8.3:8088/cluster/nodes</div></pre></td></tr></table></figure></p>
<p>一切正常，集群搭建完成</p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;准备工作&quot;&gt;&lt;a href=&quot;#准备工作&quot; class=&quot;headerlink&quot; title=&quot;准备工作&quot;&gt;&lt;/a&gt;准备工作&lt;/h3&gt;&lt;p&gt;在单机的hadoop的基础上搭建集群的hadoop&lt;/p&gt;
&lt;p&gt;先克隆3台虚拟机：&lt;br&gt;在VMware中，右键虚拟机（已关闭状态），依次选择&lt;br&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;[管理]-&amp;gt;[克隆]-&amp;gt;[下一步]-&amp;gt;[虚拟机中当前状态]-&amp;gt;[下一步]&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
&lt;p&gt;如图所示&lt;/p&gt;
&lt;div align=&quot;center&quot;&gt;&lt;br&gt;&lt;img src=&quot;/images/hadoop_001.PNG&quot; alt=&quot;hadoop_001&quot;&gt;&lt;br&gt;&lt;/div&gt;

&lt;p&gt;选择位置后克隆机器，克隆2~3台虚拟机即可&lt;/p&gt;
&lt;h3 id=&quot;修改网卡IP主机名&quot;&gt;&lt;a href=&quot;#修改网卡IP主机名&quot; class=&quot;headerlink&quot; title=&quot;修改网卡IP主机名&quot;&gt;&lt;/a&gt;修改网卡IP主机名&lt;/h3&gt;&lt;p&gt;克隆的机器叫做02，先右键机器-&amp;gt;设置-&amp;gt;网络适配器-&amp;gt;高级，给02生成一个新的mac地址&lt;/p&gt;
&lt;div align=&quot;center&quot;&gt;&lt;br&gt;&lt;img src=&quot;/images/hadoop_002.PNG&quot; alt=&quot;hadoop_002&quot;&gt;&lt;br&gt;&lt;/div&gt;

&lt;p&gt;更改规则&lt;br&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;vim /etc/udev/rules.d/70-persistent-net.rules&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
&lt;div align=&quot;center&quot;&gt;&lt;br&gt;&lt;img src=&quot;/images/hadoop_003.PNG&quot; alt=&quot;hadoop_003&quot;&gt;&lt;br&gt;&lt;/div&gt;

&lt;p&gt;删除多余的网卡，将第二个网卡的NAME改为eth0&lt;/p&gt;
&lt;p&gt;配置IP地址&lt;br&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;vim /etc/sysconfig/network-scripts/ifcfg-eth0&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
&lt;p&gt;将IP地址修改为和01机器在同一个网段下，保证01和02相互能ping通&lt;br&gt;重启网络&lt;code&gt;service network restart&lt;/code&gt;，测试一下ping，02与Windows和01都能互相ping通&lt;/p&gt;
&lt;p&gt;和搭建伪分布式一样，修改主机名和hosts文件，修改完成&lt;br&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;vim /etc/sysconfig/network&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;vim /etc/hosts&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="hadoop" scheme="http://zhangchaozc.cn/categories/hadoop/"/>
    
    
      <category term="大数据" scheme="http://zhangchaozc.cn/tags/%E5%A4%A7%E6%95%B0%E6%8D%AE/"/>
    
  </entry>
  
  <entry>
    <title>Linux安装MySQL并远程连接</title>
    <link href="http://zhangchaozc.cn/2018/10/12/%E6%95%B0%E6%8D%AE%E5%BA%93/Linux%E5%AE%89%E8%A3%85MySQL%E5%B9%B6%E8%BF%9C%E7%A8%8B%E8%BF%9E%E6%8E%A5/"/>
    <id>http://zhangchaozc.cn/2018/10/12/数据库/Linux安装MySQL并远程连接/</id>
    <published>2018-10-12T01:55:09.000Z</published>
    <updated>2018-10-12T01:57:12.794Z</updated>
    
    <content type="html"><![CDATA[<h3 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h3><p>64位的机器，先去官网下载安装包<code>mysql-5.5.61-linux-glibc2.12-x86_64.tar.gz</code><br>将安装包上传到Linux机器上<br>移动到目录，<code>mv xxx /usr/local</code><br>解压，<code>tar -zxvf xxx</code><br>多了一个<code>mysql-5.5.61-linux-glibc2.12-x86_64</code>的目录<br>将目录重命名成mysql，<code>mv xxx mysql</code>，这样更方便操作</p>
<h3 id="添加用户"><a href="#添加用户" class="headerlink" title="添加用户"></a>添加用户</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">groupadd mysql</div><div class="line">useradd -g mysql mysql</div></pre></td></tr></table></figure>
<p>添加了一个mysql的用户组和全局的用户</p>
<a id="more"></a>
<h3 id="初始化命令"><a href="#初始化命令" class="headerlink" title="初始化命令"></a>初始化命令</h3><p>cd到mysql目录<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"># 新建一个文件夹放数据</div><div class="line">mkdir ./data/mysql</div><div class="line"># 更改当前目录的权限</div><div class="line">chown -R mysql:mysql ./</div><div class="line"># 初始化数据库</div><div class="line">./scripts/mysql_install_db --user=mysql --datadir=/usr/local/mysql/data/mysql</div></pre></td></tr></table></figure></p>
<p>记住临时密码<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">[Note] A temporary password is generated for root@localhost: o*s#gqh)F4Ck</div></pre></td></tr></table></figure></p>
<h3 id="配置文件"><a href="#配置文件" class="headerlink" title="配置文件"></a>配置文件</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line"># 拷贝后可以用service命令来启动数据库</div><div class="line">cp support-files/mysql.server /etc/init.d/mysqld</div><div class="line">chmod 755 /etc/init.d/mysqld</div><div class="line"># 删除默认的配置文件或者将mysql里面的cnf文件拷贝一份到etc下，没什么影响</div><div class="line">rm -rf /etc/my.cnf</div><div class="line"># 修改启动脚本</div><div class="line">vim /etc/init.d/mysqld</div><div class="line">basedir=/usr/local/mysql/</div><div class="line">datadir=/usr/local/mysql/data/mysql</div></pre></td></tr></table></figure>
<h3 id="启动"><a href="#启动" class="headerlink" title="启动"></a>启动</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"># 启动mysql</div><div class="line">service mysqld start</div><div class="line"># 关闭mysql</div><div class="line">service mysqld stop</div><div class="line"># 查看运行状态</div><div class="line">service mysqld status</div></pre></td></tr></table></figure>
<h3 id="加入环境变量"><a href="#加入环境变量" class="headerlink" title="加入环境变量"></a>加入环境变量</h3><p>可以在任意目录下使用mysql命令<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"># G跳到文件末尾行</div><div class="line">vim /etc/profile</div><div class="line">export PATH=$PATH:/usr/local/mysql/bin</div><div class="line">source /etc/profile</div></pre></td></tr></table></figure></p>
<h3 id="修改一下密码"><a href="#修改一下密码" class="headerlink" title="修改一下密码"></a>修改一下密码</h3><p>用临时密码登录，<code>mysql -u root -p</code><br>或者直接<code>mysql uroot</code>登录<br>执行修改密码，<code>set password for root@localhost = password(&#39;123456&#39;);</code></p>
<h3 id="出现的问题"><a href="#出现的问题" class="headerlink" title="出现的问题"></a>出现的问题</h3><p>命令行可以登录，但是同局域网下的Windows无法用Navicat连接</p>
<p>登录mysql后输入如下命令：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"># &apos;123456&apos;是密码</div><div class="line">grant all privileges on *.* to &apos;root&apos;@&apos;%&apos; identified by &apos;123456&apos;;</div><div class="line">flush privileges;</div></pre></td></tr></table></figure></p>
<p>防火墙设置<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"># 开放3306端口并重启防火墙</div><div class="line">/sbin/iptables -I INPUT -p tcp --dport 3306 -j ACCEPT</div><div class="line"># 保存配置</div><div class="line">/etc/rc.d/init.d/iptables save</div><div class="line">#重启服务</div><div class="line">/etc/rc.d/init.d/iptables restart</div><div class="line"># 查看已开放端口</div><div class="line">/etc/init.d/iptables status</div></pre></td></tr></table></figure></p>
<p>在Navicat上就可以连接了</p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;准备工作&quot;&gt;&lt;a href=&quot;#准备工作&quot; class=&quot;headerlink&quot; title=&quot;准备工作&quot;&gt;&lt;/a&gt;准备工作&lt;/h3&gt;&lt;p&gt;64位的机器，先去官网下载安装包&lt;code&gt;mysql-5.5.61-linux-glibc2.12-x86_64.tar.gz&lt;/code&gt;&lt;br&gt;将安装包上传到Linux机器上&lt;br&gt;移动到目录，&lt;code&gt;mv xxx /usr/local&lt;/code&gt;&lt;br&gt;解压，&lt;code&gt;tar -zxvf xxx&lt;/code&gt;&lt;br&gt;多了一个&lt;code&gt;mysql-5.5.61-linux-glibc2.12-x86_64&lt;/code&gt;的目录&lt;br&gt;将目录重命名成mysql，&lt;code&gt;mv xxx mysql&lt;/code&gt;，这样更方便操作&lt;/p&gt;
&lt;h3 id=&quot;添加用户&quot;&gt;&lt;a href=&quot;#添加用户&quot; class=&quot;headerlink&quot; title=&quot;添加用户&quot;&gt;&lt;/a&gt;添加用户&lt;/h3&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;groupadd mysql&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;useradd -g mysql mysql&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;添加了一个mysql的用户组和全局的用户&lt;/p&gt;
    
    </summary>
    
      <category term="数据库" scheme="http://zhangchaozc.cn/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    
    
      <category term="mysql" scheme="http://zhangchaozc.cn/tags/mysql/"/>
    
  </entry>
  
  <entry>
    <title>hadoop入门wc程序</title>
    <link href="http://zhangchaozc.cn/2018/07/05/hadoop/hadoop%E5%85%A5%E9%97%A8wc%E7%A8%8B%E5%BA%8F/"/>
    <id>http://zhangchaozc.cn/2018/07/05/hadoop/hadoop入门wc程序/</id>
    <published>2018-07-05T13:40:13.000Z</published>
    <updated>2018-10-27T14:05:34.963Z</updated>
    
    <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>安装好hadoop后，需要测试启动，这里就跑一个例子。WC指的是Linux的一条指令wordcount，用来统计文件中的单词数。</p>
<p>命令：<br><code>wc [文件名]</code></p>
<p>那么如何做大数据的统计？利用hadoop来做统计，指令在<code>hadoop-mapreduce-examples-2.8.4.jar</code>包中。</p>
<h3 id="上传-下载-删除文件"><a href="#上传-下载-删除文件" class="headerlink" title="上传/下载/删除文件"></a>上传/下载/删除文件</h3><p>启动hadoop后，先传一个文件test到HDFS上：<br><code>hadoop fs -put /root/Desktop/test.tar.gz hdfs://zc01:9000/test</code></p>
<p>下载某个文件可以用界面或者命令行：<br><code>hadoop fs -get hdfs://zc01:9000/test /home</code> # 下载到home文件夹</p>
<p>删除文件（夹）：<br><code>hadoop fs -rm hdfs://zc01:9000/test</code></p>
<p>这是基础知识。</p>
<a id="more"></a>
<h3 id="利用自带的工具包做wc统计"><a href="#利用自带的工具包做wc统计" class="headerlink" title="利用自带的工具包做wc统计"></a>利用自带的工具包做wc统计</h3><p>先进入jar包目录：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">[root@zc01 mapreduce]# pwd</div><div class="line">/hadoop/hadoop-2.8.4/share/hadoop/mapreduce</div></pre></td></tr></table></figure></p>
<p>使用命令：<br><code>hadoop jar hadoop-mapreduce-examples-2.8.4.jar wordcount [输入] [输出]</code></p>
<p>其中输入和输出都是HDFS里面的地址，则先把待统计的words.txt文件上传到HDFS中：<br><code>hadoop fs -put /root/Desktop/words.txt hdfs://zc01:9000/words</code></p>
<p>然后计算：<br><code>hadoop jar hadoop-mapreduce-examples-2.8.4.jar wordcount hdfs://zc01:9000/words hdfs://zc01:9000/out</code></p>
<p>计算完成，<code>hadoop fs -ls /</code>列出目录，发现多了out目录，也可以直接界面查看计算结果。</p>
<div align="center"><br><img src="/images/hadoop1-3.PNG" alt="hadoop1-3"><br></div>

<p>下载计算结果时浏览器无法解析主机名，应该在hosts文件（C:\Windows\System32\drivers\etc\hosts）中添加映射（相当于本机的DNS）：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">192.168.8.88	zc01</div></pre></td></tr></table></figure></p>
<p>下载part-r-00000文件，打开后即是wc统计的结果。</p>
<h3 id="自己写一个简单的WordCount程序"><a href="#自己写一个简单的WordCount程序" class="headerlink" title="自己写一个简单的WordCount程序"></a>自己写一个简单的WordCount程序</h3><p>新建maven程序，pom依赖<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">properties</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">project.build.sourceEncoding</span>&gt;</span>UTF-8<span class="tag">&lt;/<span class="name">project.build.sourceEncoding</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">hadoopVersion</span>&gt;</span>2.8.4<span class="tag">&lt;/<span class="name">hadoopVersion</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">properties</span>&gt;</span></div><div class="line"></div><div class="line"><span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></div><div class="line">    <span class="comment">&lt;!-- Hadoop start --&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.hadoop<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>hadoop-common<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>$&#123;hadoopVersion&#125;<span class="tag">&lt;/<span class="name">version</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.hadoop<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>hadoop-hdfs<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>$&#123;hadoopVersion&#125;<span class="tag">&lt;/<span class="name">version</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.hadoop<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>hadoop-mapreduce-client-core<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>$&#123;hadoopVersion&#125;<span class="tag">&lt;/<span class="name">version</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.hadoop<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>hadoop-client<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>$&#123;hadoopVersion&#125;<span class="tag">&lt;/<span class="name">version</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>junit<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>junit<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p>自己写一个WordCount类<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div><div class="line">43</div><div class="line">44</div><div class="line">45</div><div class="line">46</div><div class="line">47</div><div class="line">48</div><div class="line">49</div><div class="line">50</div><div class="line">51</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">WordCount</span> </span>&#123;</div><div class="line">    <span class="comment">//实现一个Mapper类</span></div><div class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">TokenizerMapper</span> <span class="keyword">extends</span> <span class="title">Mapper</span>&lt;<span class="title">Object</span>, <span class="title">Text</span>, <span class="title">Text</span>, <span class="title">IntWritable</span>&gt; </span>&#123;</div><div class="line">        <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">static</span> IntWritable one = <span class="keyword">new</span> IntWritable(<span class="number">1</span>);</div><div class="line">        <span class="keyword">private</span> Text word = <span class="keyword">new</span> Text();</div><div class="line"></div><div class="line">        <span class="meta">@Override</span></div><div class="line">        <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">map</span><span class="params">(Object key, Text value, Context context)</span> <span class="keyword">throws</span> IOException, InterruptedException </span>&#123;</div><div class="line">            StringTokenizer itr = <span class="keyword">new</span> StringTokenizer(value.toString());</div><div class="line">            <span class="keyword">while</span> (itr.hasMoreTokens()) &#123;</div><div class="line">                word.set(itr.nextToken());</div><div class="line">                context.write(word, one);</div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="comment">//实现一个Reducer类</span></div><div class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">IntSumReducer</span> <span class="keyword">extends</span> <span class="title">Reducer</span>&lt;<span class="title">Text</span>, <span class="title">IntWritable</span>, <span class="title">Text</span>, <span class="title">IntWritable</span>&gt; </span>&#123;</div><div class="line">        <span class="keyword">private</span> IntWritable result = <span class="keyword">new</span> IntWritable();</div><div class="line"></div><div class="line">        <span class="meta">@Override</span></div><div class="line">        <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">reduce</span><span class="params">(Text key, Iterable&lt;IntWritable&gt; values, Context context)</span> <span class="keyword">throws</span> IOException, InterruptedException </span>&#123;</div><div class="line">            <span class="keyword">int</span> sum = <span class="number">0</span>;</div><div class="line">            <span class="keyword">for</span> (IntWritable val : values) &#123;</div><div class="line">                sum += val.get();</div><div class="line">            &#125;</div><div class="line">            result.set(sum);</div><div class="line">            context.write(key, result);</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception </span>&#123;</div><div class="line">        Configuration conf = <span class="keyword">new</span> Configuration();</div><div class="line">        String[] otherArgs = <span class="keyword">new</span> GenericOptionsParser(conf, args).getRemainingArgs();</div><div class="line">        <span class="keyword">if</span> (otherArgs.length != <span class="number">2</span>) &#123;</div><div class="line">            System.err.println(<span class="string">"Error in args"</span>);</div><div class="line">            System.exit(<span class="number">2</span>);</div><div class="line">        &#125;</div><div class="line">        Job job = Job.getInstance(conf, <span class="string">"word count"</span>);</div><div class="line">        job.setJarByClass(WordCount.class);</div><div class="line">        job.setMapperClass(TokenizerMapper.class);</div><div class="line">        job.setReducerClass(IntSumReducer.class);</div><div class="line">        job.setOutputKeyClass(Text.class);</div><div class="line">        job.setOutputValueClass(IntWritable.class);</div><div class="line">        <span class="comment">//输入目录</span></div><div class="line">        FileInputFormat.addInputPath(job, <span class="keyword">new</span> Path(otherArgs[<span class="number">0</span>]));</div><div class="line">        <span class="comment">//输出目录</span></div><div class="line">        FileOutputFormat.setOutputPath(job, <span class="keyword">new</span> Path(otherArgs[<span class="number">1</span>]));</div><div class="line">        System.exit(job.waitForCompletion(<span class="keyword">true</span>) ? <span class="number">0</span> : <span class="number">1</span>);</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>在弄清楚MapReduce的原理的基础上，自己实现一个Map类和Reducer类。</p>
<p>将项目打包成jar文件，上传到hdfs中，利用命令行运行<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">hadoop jar hadoop01.jar demo01.WordCount hdfs://hadoop01:9000/wc_test_01 hdfs://hadoop01:9000/wc_out2</div></pre></td></tr></table></figure></p>
<p>执行结果与example包中计算的结果一样（程序无误的前提下），这样，就完成了简单的WordCount程序</p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h3&gt;&lt;p&gt;安装好hadoop后，需要测试启动，这里就跑一个例子。WC指的是Linux的一条指令wordcount，用来统计文件中的单词数。&lt;/p&gt;
&lt;p&gt;命令：&lt;br&gt;&lt;code&gt;wc [文件名]&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;那么如何做大数据的统计？利用hadoop来做统计，指令在&lt;code&gt;hadoop-mapreduce-examples-2.8.4.jar&lt;/code&gt;包中。&lt;/p&gt;
&lt;h3 id=&quot;上传-下载-删除文件&quot;&gt;&lt;a href=&quot;#上传-下载-删除文件&quot; class=&quot;headerlink&quot; title=&quot;上传/下载/删除文件&quot;&gt;&lt;/a&gt;上传/下载/删除文件&lt;/h3&gt;&lt;p&gt;启动hadoop后，先传一个文件test到HDFS上：&lt;br&gt;&lt;code&gt;hadoop fs -put /root/Desktop/test.tar.gz hdfs://zc01:9000/test&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;下载某个文件可以用界面或者命令行：&lt;br&gt;&lt;code&gt;hadoop fs -get hdfs://zc01:9000/test /home&lt;/code&gt; # 下载到home文件夹&lt;/p&gt;
&lt;p&gt;删除文件（夹）：&lt;br&gt;&lt;code&gt;hadoop fs -rm hdfs://zc01:9000/test&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;这是基础知识。&lt;/p&gt;
    
    </summary>
    
      <category term="hadoop" scheme="http://zhangchaozc.cn/categories/hadoop/"/>
    
    
      <category term="大数据" scheme="http://zhangchaozc.cn/tags/%E5%A4%A7%E6%95%B0%E6%8D%AE/"/>
    
  </entry>
  
  <entry>
    <title>搭建伪分布式hadoop</title>
    <link href="http://zhangchaozc.cn/2018/07/05/hadoop/%E6%90%AD%E5%BB%BA%E4%BC%AA%E5%88%86%E5%B8%83%E5%BC%8Fhadoop/"/>
    <id>http://zhangchaozc.cn/2018/07/05/hadoop/搭建伪分布式hadoop/</id>
    <published>2018-07-05T13:39:33.000Z</published>
    <updated>2018-10-16T08:20:59.754Z</updated>
    
    <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>学习hadoop的第一课，先搭建好伪分布式环境。</p>
<h5 id="hadoop是什么，能干什么？"><a href="#hadoop是什么，能干什么？" class="headerlink" title="hadoop是什么，能干什么？"></a>hadoop是什么，能干什么？</h5><p>解决问题：</p>
<ul>
<li>海量数据的存储（HDFS），分布式存储系统</li>
<li>海量数据的分析（MapReduce），分布式计算模型</li>
</ul>
<p>能干什么？<br>大数据存储，大数据分析</p>
<h5 id="hadoop2-x"><a href="#hadoop2-x" class="headerlink" title="hadoop2.x"></a>hadoop2.x</h5><p>提供了YERN（资源调度系统），可以运行各种计算模型。<br>可以运行自家的MapReduce（处理离线数据），也可以运行Storm、Spark等处理实时数据。</p>
<h3 id="基础理论"><a href="#基础理论" class="headerlink" title="基础理论"></a>基础理论</h3><h5 id="HDFS架构"><a href="#HDFS架构" class="headerlink" title="HDFS架构"></a>HDFS架构</h5><p>主节点，只有一个：namenode，2.0版本之后有多个，相当于老大，作用：</p>
<ul>
<li>接收用户操作请求</li>
<li>维护文件系统的目录结构</li>
<li>管理文件与block之间关系、block与datanode之间关系</li>
</ul>
<p>从节点，有很多个：datanodes，作用：</p>
<ul>
<li>存储文件，文件被分成block存储在磁盘上</li>
<li>为保证数据安全，文件会有多个副本</li>
</ul>
<h5 id="部署方式"><a href="#部署方式" class="headerlink" title="部署方式"></a>部署方式</h5><ul>
<li>本地模式</li>
<li>伪分布式模式</li>
<li>集群模式</li>
</ul>
<h3 id="安装步骤："><a href="#安装步骤：" class="headerlink" title="安装步骤："></a>安装步骤：</h3><ol>
<li>准备工作（关防火墙，改ip、hostname）</li>
<li>安装（JDK、hadoop）</li>
<li>启动测试（ssh免登陆等）</li>
</ol>
<a id="more"></a>
<h3 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h3><h5 id="改ip、hostname"><a href="#改ip、hostname" class="headerlink" title="改ip、hostname"></a>改ip、hostname</h5><p>先查看下ip和主机名：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">ifconfig</div><div class="line">hostname</div></pre></td></tr></table></figure></p>
<p>修改ip有三种方式：</p>
<ol>
<li>图形界面修改。设置IPv4 method为Manual，这样每次启动后虚拟机的IP不会改变。</li>
<li>setup命令。（redhat旗下专属）</li>
<li>修改配置文件。</li>
</ol>
<p>修改配置：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">vim /etc/sysconfig/network-scripts/ifcfg-eth0</div></pre></td></tr></table></figure></p>
<p>修改BOOTPROTO=”static”，增加<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">IPADDR=&quot;192.168.8.88&quot;</div><div class="line">NETMASK=&quot;255.255.255.0&quot;</div><div class="line">GATEWAY=&quot;192.168.8.1&quot;</div><div class="line"># 上网DNS</div><div class="line">DNS1=&quot;8.8.8.8&quot;</div><div class="line">DNS2=&quot;8.8.4.4&quot;</div></pre></td></tr></table></figure></p>
<p>修改主机名：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">vim /etc/sysconfig/network</div><div class="line">HOSTNAME=zc01    ###</div></pre></td></tr></table></figure></p>
<p>修改IP与主机名的映射关系（host文件）：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">vim /etc/hosts</div><div class="line">192.168.8.88	zc01</div></pre></td></tr></table></figure></p>
<p>为了确保主机能与虚拟机进行通信，虚拟机的配置应使用<code>hostonly</code>模式：</p>
<div align="center"><br><img src="/images/VM01.PNG" alt="VM01"><br></div>

<p>设置网络配置的vmnetcfg.exe和VW启动的exe在同一目录。</p>
<p>设置里面也要进行配置：</p>
<div align="center"><br><img src="/images/VM02.PNG" alt="VM02"><br></div>

<p>设置Win10的IP就是走VMnet1这个网卡，需要设置Win10的IP：</p>
<div align="center"><br><img src="/images/VM03.PNG" alt="VM03"><br></div>

<p>虚拟机的IP为192.168.8.88，Win10的IP为192.168.8.100。<br>设置完成后相互能<code>ping</code>通，说明可以通信。</p>
<h5 id="关闭防火墙"><a href="#关闭防火墙" class="headerlink" title="关闭防火墙"></a>关闭防火墙</h5><p>为了集群之间能更好的通信。</p>
<p>查看一下防火墙状态：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">service iptables status</div></pre></td></tr></table></figure></p>
<p>有信息说明是开启的。<br>关闭服务：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">service iptables stop</div></pre></td></tr></table></figure></p>
<p>这样只是<code>暂时</code>关闭了！不能保证机器重启时防火墙服务依然开启。那么应该查看启动状态：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">chkconfig iptables --list</div></pre></td></tr></table></figure></p>
<p>打印出：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">[root@localhost Desktop]# chkconfig iptables --list</div><div class="line">iptables       	0:off	1:off	2:on	3:on	4:on	5:on	6:off</div></pre></td></tr></table></figure></p>
<p>可以看到当启动状态为2~5时（之前的基础知识）服务会启动，因此我们要防止此服务在启动linux时自动开启。</p>
<p>关闭防火墙开机启动：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">chkconfig iptables off</div></pre></td></tr></table></figure></p>
<p>再次查看系统状态就发现都是off了。</p>
<p>都配置完后<code>reboot</code>重启一下机器。</p>
<h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><h5 id="JDK"><a href="#JDK" class="headerlink" title="JDK"></a>JDK</h5><p>安装JDK，这是经常操作了，过一遍就行。</p>
<ol>
<li>创建新文件夹<code>mkdir /usr/java</code>，这个一个好习惯。</li>
<li>下载、解压<code>tar -zxvf xxx</code>等。</li>
<li>环境变量。</li>
</ol>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">vim /etc/profile</div><div class="line"># 在文件最后添加</div><div class="line">export JAVA_HOME=/usr/java/jdk1.8.0_171</div><div class="line">export PATH=$PATH:$JAVA_HOME/bin</div></pre></td></tr></table></figure>
<p>最后使配置生效<code>source /etc/profile</code>，我安装的1.8版本的JDK。</p>
<h5 id="安装hadoop"><a href="#安装hadoop" class="headerlink" title="安装hadoop"></a>安装hadoop</h5><p>根目录下新建<code>/hadoop</code>目录，下载二进制包到此目录下。<br>解压<code>tar -zxvf hadoop-2.8.4.tar.gz</code>。</p>
<p><a href="http://hadoop.apache.org/releases.html" target="_blank" rel="external">下载地址</a>，我是下载的2.8.4版本的二进制包。</p>
<p>修改配置文件，一个是修改5个：</p>
<p>以下配置文件都是在一个目录下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line">[root@zc01 etc]# cd /hadoop/hadoop-2.8.4/etc/hadoop/</div><div class="line">[root@zc01 hadoop]# ls</div><div class="line">capacity-scheduler.xml      httpfs-env.sh            mapred-env.sh</div><div class="line">configuration.xsl           httpfs-log4j.properties  mapred-queues.xml.template</div><div class="line">container-executor.cfg      httpfs-signature.secret  mapred-site.xml</div><div class="line">core-site.xml               httpfs-site.xml          slaves</div><div class="line">hadoop-env.cmd              kms-acls.xml             ssl-client.xml.example</div><div class="line">hadoop-env.sh               kms-env.sh               ssl-server.xml.example</div><div class="line">hadoop-metrics2.properties  kms-log4j.properties     yarn-env.cmd</div><div class="line">hadoop-metrics.properties   kms-site.xml             yarn-env.sh</div><div class="line">hadoop-policy.xml           log4j.properties         yarn-site.xml</div><div class="line">hdfs-site.xml               mapred-env.cmd</div></pre></td></tr></table></figure></p>
<p>都是用vim直接编辑修改，在Linux基础博客有讲到vim。</p>
<h6 id="hadoop-env-sh"><a href="#hadoop-env-sh" class="headerlink" title="hadoop-env.sh"></a>hadoop-env.sh</h6><p>修改JAVA_HOME变量：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">#第27行</div><div class="line">export JAVA_HOME=/usr/java/jdk1.8.0_171</div><div class="line">`</div></pre></td></tr></table></figure></p>
<h6 id="core-site-xml"><a href="#core-site-xml" class="headerlink" title="core-site.xml"></a>core-site.xml</h6><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line">&lt;!-- 指定HDFS的老大（NameNode）的地址 --&gt;</div><div class="line">&lt;property&gt;</div><div class="line">    &lt;name&gt;fs.defaultFS&lt;/name&gt;</div><div class="line">    &lt;value&gt;hdfs://192.168.8.88:9000&lt;/value&gt;</div><div class="line">&lt;/property&gt;</div><div class="line">&lt;!-- 指定hadoop运行时产生文件的存储目录 --&gt;</div><div class="line">&lt;property&gt;</div><div class="line">    &lt;name&gt;hadoop.tmp.dir&lt;/name&gt;</div><div class="line">    &lt;value&gt;/hadoop/hadoop-2.8.4/tmp&lt;/value&gt;</div><div class="line">&lt;/property&gt;</div></pre></td></tr></table></figure>
<h6 id="hdfs-site-xml"><a href="#hdfs-site-xml" class="headerlink" title="hdfs-site.xml"></a>hdfs-site.xml</h6><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">&lt;!-- 指定HDFS副本的数量（包括自己，集群时默认为3） --&gt;</div><div class="line">&lt;property&gt;</div><div class="line">    &lt;name&gt;dfs.replication&lt;/name&gt;</div><div class="line">    &lt;value&gt;1&lt;/value&gt;</div><div class="line">&lt;/property&gt;</div></pre></td></tr></table></figure>
<h6 id="mapred-site-xml"><a href="#mapred-site-xml" class="headerlink" title="mapred-site.xml"></a>mapred-site.xml</h6><p>此文件不存在，先重命名一下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">mv mapred-site.xml.template mapred-site.xml</div><div class="line">vim mapred-site.xml</div></pre></td></tr></table></figure></p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">&lt;!-- 指定mr运行在yarn上 --&gt;</div><div class="line">&lt;property&gt;</div><div class="line">    &lt;name&gt;mapreduce.framework.name&lt;/name&gt;</div><div class="line">    &lt;value&gt;yarn&lt;/value&gt;</div><div class="line">&lt;/property&gt;</div></pre></td></tr></table></figure>
<h6 id="yarn-site-xml"><a href="#yarn-site-xml" class="headerlink" title="yarn-site.xml"></a>yarn-site.xml</h6><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line">&lt;!-- reducer取数据的方式是mapreduce_shuffle --&gt;</div><div class="line">&lt;property&gt;</div><div class="line">    &lt;name&gt;yarn.nodemanager.aux-services&lt;/name&gt;</div><div class="line">    &lt;value&gt;mapreduce_shuffle&lt;/value&gt;</div><div class="line">&lt;/property&gt;</div><div class="line">&lt;!--指定YARN的老大ResourceManager的地址--&gt;</div><div class="line">&lt;property&gt;</div><div class="line">    &lt;name&gt;yarn.resourcemanager.hostname&lt;/name&gt;</div><div class="line">    &lt;value&gt;zc01&lt;/value&gt;</div><div class="line">&lt;/property&gt;</div></pre></td></tr></table></figure>
<p>最后将hadoop添加到环境变量中：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">vim /etc/profile</div><div class="line">export HADOOP_HOME=/hadoop/hadoop-2.8.4</div><div class="line">export PATH=$PATH:$HADOOP_HOME/bin</div><div class="line">source /etc/profile</div></pre></td></tr></table></figure></p>
<p>至此，已经完成了hadoop的安装工作。</p>
<h3 id="启动hadoop"><a href="#启动hadoop" class="headerlink" title="启动hadoop"></a>启动hadoop</h3><p>初始化HDFS（namenode）第一次使用时要格式化：<br><code>hadoop namenode -format</code>，打印出如下说明格式化成功：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">18/07/04 19:47:06 INFO common.Storage: Storage directory /hadoop/hadoop-2.8.4/tmp/dfs/name has been successfully formatted.</div></pre></td></tr></table></figure></p>
<p>启动hadoop，先启动HDFS，<code>cd /hadoop/hadoop-2.8.4</code>切换到hadoop目录：<br><code>sbin/start-dfs.sh</code>启动DFS，<code>sbin/start-yarn.sh</code>再启动YARN。</p>
<p>也可以直接启动全部：<code>sbin/start-all.sh</code>（过时）</p>
<p>判断是否启动成功？<br>命令<code>jps</code>打印出Java进程，共有如下进程：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">6624 SecondaryNameNode</div><div class="line">7042 NodeManager</div><div class="line">6394 DataNode</div><div class="line">6301 NameNode</div><div class="line">6766 ResourceManager</div><div class="line">7070 Jps</div></pre></td></tr></table></figure></p>
<p>说明启动成功！</p>
<p>也可以在Windows的浏览器直接访问：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">http://192.168.8.88:50070  (HDFS管理界面)</div><div class="line">http://192.168.8.88:8088 （MR管理界面）</div></pre></td></tr></table></figure></p>
<p>如图：</p>
<div align="center"><br><img src="/images/hadoop1-1.PNG" alt="hadoop1-1"><br></div>

<div align="center"><br><img src="/images/hadoop1-2.PNG" alt="hadoop1-2"><br></div>

<p>说明hadoop伪分布式环境搭建完成！</p>
<h3 id="解决输入多次密码的问题"><a href="#解决输入多次密码的问题" class="headerlink" title="解决输入多次密码的问题"></a>解决输入多次密码的问题</h3><p>在启动和停止hadoop的指令中，需要输入多次密码，一台机器还是可以忍受的。</p>
<p>如果是很多机器，每次输入密码太麻烦了，如何免密码启动？</p>
<p>解决：配置ssh免登陆：<br><code>/root</code>目录下有隐藏的<code>.ssh</code>目录，先cd到这个目录，生成秘钥：<br><code>ssh-keygen -t rsa -C &quot;zhang0chao0@gmail.com&quot;</code><br>一路回车即可。</p>
<p>设置自己对自己免登陆，一行命令：<br><code>cp id_rsa.pub authorized_keys</code>即可。</p>
<p>拓展：<br>想免登录密码登录其它的机器，一行命令<code>ssh-copy-id [IP地址]</code>，把自己的公钥拷贝到要登录的机器上即可免秘钥登录。</p>
<h3 id="启动过程中的异常"><a href="#启动过程中的异常" class="headerlink" title="启动过程中的异常"></a>启动过程中的异常</h3><p>可以查看日志来解决。<br><code>cd /hadoop/hadoop-2.8.4/logs</code>，相关的日志都在这个文件夹中。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h3&gt;&lt;p&gt;学习hadoop的第一课，先搭建好伪分布式环境。&lt;/p&gt;
&lt;h5 id=&quot;hadoop是什么，能干什么？&quot;&gt;&lt;a href=&quot;#hadoop是什么，能干什么？&quot; class=&quot;headerlink&quot; title=&quot;hadoop是什么，能干什么？&quot;&gt;&lt;/a&gt;hadoop是什么，能干什么？&lt;/h5&gt;&lt;p&gt;解决问题：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;海量数据的存储（HDFS），分布式存储系统&lt;/li&gt;
&lt;li&gt;海量数据的分析（MapReduce），分布式计算模型&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;能干什么？&lt;br&gt;大数据存储，大数据分析&lt;/p&gt;
&lt;h5 id=&quot;hadoop2-x&quot;&gt;&lt;a href=&quot;#hadoop2-x&quot; class=&quot;headerlink&quot; title=&quot;hadoop2.x&quot;&gt;&lt;/a&gt;hadoop2.x&lt;/h5&gt;&lt;p&gt;提供了YERN（资源调度系统），可以运行各种计算模型。&lt;br&gt;可以运行自家的MapReduce（处理离线数据），也可以运行Storm、Spark等处理实时数据。&lt;/p&gt;
&lt;h3 id=&quot;基础理论&quot;&gt;&lt;a href=&quot;#基础理论&quot; class=&quot;headerlink&quot; title=&quot;基础理论&quot;&gt;&lt;/a&gt;基础理论&lt;/h3&gt;&lt;h5 id=&quot;HDFS架构&quot;&gt;&lt;a href=&quot;#HDFS架构&quot; class=&quot;headerlink&quot; title=&quot;HDFS架构&quot;&gt;&lt;/a&gt;HDFS架构&lt;/h5&gt;&lt;p&gt;主节点，只有一个：namenode，2.0版本之后有多个，相当于老大，作用：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;接收用户操作请求&lt;/li&gt;
&lt;li&gt;维护文件系统的目录结构&lt;/li&gt;
&lt;li&gt;管理文件与block之间关系、block与datanode之间关系&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;从节点，有很多个：datanodes，作用：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;存储文件，文件被分成block存储在磁盘上&lt;/li&gt;
&lt;li&gt;为保证数据安全，文件会有多个副本&lt;/li&gt;
&lt;/ul&gt;
&lt;h5 id=&quot;部署方式&quot;&gt;&lt;a href=&quot;#部署方式&quot; class=&quot;headerlink&quot; title=&quot;部署方式&quot;&gt;&lt;/a&gt;部署方式&lt;/h5&gt;&lt;ul&gt;
&lt;li&gt;本地模式&lt;/li&gt;
&lt;li&gt;伪分布式模式&lt;/li&gt;
&lt;li&gt;集群模式&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;安装步骤：&quot;&gt;&lt;a href=&quot;#安装步骤：&quot; class=&quot;headerlink&quot; title=&quot;安装步骤：&quot;&gt;&lt;/a&gt;安装步骤：&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;准备工作（关防火墙，改ip、hostname）&lt;/li&gt;
&lt;li&gt;安装（JDK、hadoop）&lt;/li&gt;
&lt;li&gt;启动测试（ssh免登陆等）&lt;/li&gt;
&lt;/ol&gt;
    
    </summary>
    
      <category term="hadoop" scheme="http://zhangchaozc.cn/categories/hadoop/"/>
    
    
      <category term="大数据" scheme="http://zhangchaozc.cn/tags/%E5%A4%A7%E6%95%B0%E6%8D%AE/"/>
    
  </entry>
  
  <entry>
    <title>linux基础5-网络命令</title>
    <link href="http://zhangchaozc.cn/2018/02/13/linux/linux%E5%9F%BA%E7%A1%805-%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4/"/>
    <id>http://zhangchaozc.cn/2018/02/13/linux/linux基础5-网络命令/</id>
    <published>2018-02-13T04:13:09.000Z</published>
    <updated>2018-02-13T05:43:42.643Z</updated>
    
    <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>主要总结一下基础的网络命令。</p>
<h3 id="网络管理"><a href="#网络管理" class="headerlink" title="网络管理"></a>网络管理</h3><h5 id="who"><a href="#who" class="headerlink" title="who"></a>who</h5><p>查看用户的登录信息</p>
<h5 id="w"><a href="#w" class="headerlink" title="w"></a>w</h5><p>和<code>who</code>一样，显示结果中：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">tty：代表本地登录</div><div class="line">pts：代表远程终端登录</div></pre></td></tr></table></figure></p>
<h5 id="write"><a href="#write" class="headerlink" title="write"></a>write</h5><p>简单的即时通信，给用户发信息，以<code>Ctrl+D</code>保存结束<br><code>write [用户名]</code>：用户不在登录状态是收不到的，可以用<code>w</code>命令，看看哪些用户在登录状态</p>
<h5 id="wall"><a href="#wall" class="headerlink" title="wall"></a>wall</h5><p>如何群发登录用户？<br><code>wall [消息内容]</code>：所有的登录用户都可以收到</p>
<a id="more"></a>
<h5 id="ping"><a href="#ping" class="headerlink" title="ping"></a>ping</h5><p>探测消息，网络是否通？<br>语法：<code>ping [选项] [IP地址]</code>，参数：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">-c：指定发送次数</div></pre></td></tr></table></figure></p>
<p>例如：<code>ping -c3 127.0.0.1</code></p>
<h5 id="ifconfig"><a href="#ifconfig" class="headerlink" title="ifconfig"></a>ifconfig</h5><p>查看网卡信息，windows里面叫<code>ipconfig</code>，eth0代表本地的真实网卡，主要是查看机器的IP地址</p>
<h5 id="mail"><a href="#mail" class="headerlink" title="mail"></a>mail</h5><p>邮件命令，向某一个用户发送邮件<br>语法：<code>mail [用户名]</code><br>查看邮件：<br>mail即可，help查看支持的命令格式，输入数字代表查看第几封邮件，参数：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">h：列出所有邮件</div><div class="line">d [数字]：删除某封邮件</div><div class="line">q：退出</div></pre></td></tr></table></figure></p>
<h5 id="last"><a href="#last" class="headerlink" title="last"></a>last</h5><p>列出目前与过去用户登入系统的信息，重启系统信息等</p>
<h5 id="lastlog"><a href="#lastlog" class="headerlink" title="lastlog"></a>lastlog</h5><p>列出用户最后一次登录系统的信息</p>
<h5 id="traceroute"><a href="#traceroute" class="headerlink" title="traceroute"></a>traceroute</h5><p><code>非常重要</code>的一个命令，显示数据包到主机间的路径<br>例如：<code>traceroute www.baidu.com</code></p>
<p>如果网断了，可以用这个命令查看在哪一个IP节点丢包的</p>
<h5 id="netstat"><a href="#netstat" class="headerlink" title="netstat"></a>netstat</h5><p>显示网络相关信息<br>语法：<code>netstat [选项]</code>，参数：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">-t：TCP协议</div><div class="line">-u：UDP协议</div><div class="line">-l：监听</div><div class="line">-r：路由</div><div class="line">-n：显示IP地址和端口号</div></pre></td></tr></table></figure></p>
<p>例子：<br><code>netstat -tlun</code>：查看本机监听的端口（可以查看本机开了哪些服务）<br><code>netstat -an</code>：查看本机所有的网络连接<br><code>netstat -rm</code>：查看本机路由表（查询本机网关）</p>
<h5 id="setup"><a href="#setup" class="headerlink" title="setup"></a>setup</h5><p>调出详细配置页面（红帽系列的系统才有这个命令）</p>
<p>配置完后重启网络服务：<code>service network restart</code></p>
<h5 id="mount"><a href="#mount" class="headerlink" title="mount"></a>mount</h5><p>挂载光盘，卸载光盘<br>语法：<code>mount [-t iso9660] [设备文件名] [挂载点]</code></p>
<p>例子（/dev是放设备的目录，/mnt是挂载目录，在里面新建一个自己的挂载点）：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">mount -t iso9660 /dev/sr0 /mnt/cdrom</div><div class="line">mount /dev/sr0 /mnt/cdrom，也行</div></pre></td></tr></table></figure></p>
<p>卸载光盘：<code>unmount /dev/sr0</code>，或者<code>unmount /mnt/cdrom</code></p>
<p>注意卸载时当前目录不能为挂载目录，否则会提示busy</p>
<h3 id="关机命令"><a href="#关机命令" class="headerlink" title="关机命令"></a>关机命令</h3><h5 id="shutdown"><a href="#shutdown" class="headerlink" title="shutdown"></a>shutdown</h5><p>关机重启命令（推荐shutdown，安全性高）<br>语法：<code>shutdown [选项] [时间]</code>，参数：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">-c：取消前一个关机命令</div><div class="line">-h：关机</div><div class="line">-r：重启</div></pre></td></tr></table></figure></p>
<p>其它关机命令：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">halt</div><div class="line">poweroff</div><div class="line">init 0</div><div class="line">其它重启命令</div><div class="line">reboot</div><div class="line">init 6</div></pre></td></tr></table></figure></p>
<p>启动选项，打开配置文件<code>cat /etc/inittab</code>，小知识：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">0：关机</div><div class="line">1：单用户</div><div class="line">2：不完全多用户，不含NFS文件共享服务</div><div class="line">3：完全多用户（一般服务器是这个）</div><div class="line">4：未分配</div><div class="line">5：图形界面（一般个人机，虚拟机）</div><div class="line">6：重启</div></pre></td></tr></table></figure></p>
<h5 id="runlevel"><a href="#runlevel" class="headerlink" title="runlevel"></a>runlevel</h5><p>查询当前运行级别</p>
<h5 id="logout"><a href="#logout" class="headerlink" title="logout"></a>logout</h5><p>退出登录命令（操作完服务器记得退出）</p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h3&gt;&lt;p&gt;主要总结一下基础的网络命令。&lt;/p&gt;
&lt;h3 id=&quot;网络管理&quot;&gt;&lt;a href=&quot;#网络管理&quot; class=&quot;headerlink&quot; title=&quot;网络管理&quot;&gt;&lt;/a&gt;网络管理&lt;/h3&gt;&lt;h5 id=&quot;who&quot;&gt;&lt;a href=&quot;#who&quot; class=&quot;headerlink&quot; title=&quot;who&quot;&gt;&lt;/a&gt;who&lt;/h5&gt;&lt;p&gt;查看用户的登录信息&lt;/p&gt;
&lt;h5 id=&quot;w&quot;&gt;&lt;a href=&quot;#w&quot; class=&quot;headerlink&quot; title=&quot;w&quot;&gt;&lt;/a&gt;w&lt;/h5&gt;&lt;p&gt;和&lt;code&gt;who&lt;/code&gt;一样，显示结果中：&lt;br&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;tty：代表本地登录&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;pts：代表远程终端登录&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
&lt;h5 id=&quot;write&quot;&gt;&lt;a href=&quot;#write&quot; class=&quot;headerlink&quot; title=&quot;write&quot;&gt;&lt;/a&gt;write&lt;/h5&gt;&lt;p&gt;简单的即时通信，给用户发信息，以&lt;code&gt;Ctrl+D&lt;/code&gt;保存结束&lt;br&gt;&lt;code&gt;write [用户名]&lt;/code&gt;：用户不在登录状态是收不到的，可以用&lt;code&gt;w&lt;/code&gt;命令，看看哪些用户在登录状态&lt;/p&gt;
&lt;h5 id=&quot;wall&quot;&gt;&lt;a href=&quot;#wall&quot; class=&quot;headerlink&quot; title=&quot;wall&quot;&gt;&lt;/a&gt;wall&lt;/h5&gt;&lt;p&gt;如何群发登录用户？&lt;br&gt;&lt;code&gt;wall [消息内容]&lt;/code&gt;：所有的登录用户都可以收到&lt;/p&gt;
    
    </summary>
    
      <category term="linux" scheme="http://zhangchaozc.cn/categories/linux/"/>
    
    
  </entry>
  
  <entry>
    <title>linux基础4-软件包管理</title>
    <link href="http://zhangchaozc.cn/2018/02/12/linux/linux%E5%9F%BA%E7%A1%804-%E8%BD%AF%E4%BB%B6%E5%8C%85%E7%AE%A1%E7%90%86/"/>
    <id>http://zhangchaozc.cn/2018/02/12/linux/linux基础4-软件包管理/</id>
    <published>2018-02-12T14:51:40.000Z</published>
    <updated>2018-02-13T06:31:55.794Z</updated>
    
    <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>主要学习各种软件包的管理：源码包，二进制包，rpm包。</p>
<h3 id="软件包管理"><a href="#软件包管理" class="headerlink" title="软件包管理"></a>软件包管理</h3><p>软件包分为：</p>
<ol>
<li>源代码包</li>
<li>二进制包（RPM包、系统默认包）</li>
</ol>
<h5 id="rpm"><a href="#rpm" class="headerlink" title="rpm"></a>rpm</h5><p>RPM包的依赖性查询：<code>www.rpmfind.net</code></p>
<p>手动安装rpm包的最大坏处：<br>一个包的依赖太多，a-&gt;b-&gt;c树形依赖，或者a-&gt;b-&gt;c-&gt;a环形依赖，使得安装rpm包变得很复杂</p>
<p>RPM安装：<br>语法：<code>rpm -ivh [包全名]</code>，参数：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">-i（install）：安装</div><div class="line">-v（verbose）：显示详细信息</div><div class="line">-h（hash）：显示进度</div><div class="line">--nodeps：不检测依赖性</div></pre></td></tr></table></figure></p>
<a id="more"></a>
<p>RPM包升级：<br>语法：<code>rpm -Uvh [包全名]</code>，参数：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">-U：升级</div></pre></td></tr></table></figure></p>
<p>RPM包卸载：<br>语法：<code>rpm -e [包名]</code> （这个是包名而不是全名）</p>
<p>RPM包查询（重要）：</p>
<ol>
<li>查询是否安装：<code>rpm -q [包名]</code></li>
<li>查询所有已经安装的RPM包<code>rpm -qa</code>，只看所有包中关于<code>httpd</code>的包：<code>rpm -qa | grep httpd</code></li>
<li>查询软件包详细信息：<code>rpm -qi [包名]</code>，<code>-i</code>：查询软件信息，<code>-p</code>：查询未安装包信息（指定包全名）</li>
<li>查询包中文件安装位置：<code>rpm -ql [包名]</code></li>
<li>查询系统文件属于哪个RPM包：<code>rpm -qf [系统文件名]</code></li>
<li>查询软件包的依赖性：<code>rpm -qR [包名]</code></li>
</ol>
<p>RPM包校验：<br>语法：<code>rmp -V [包名]</code><br>如果没有任何提示，说明这个包自安装以来就没被修改过</p>
<div align="center"><br><img src="/images/linux_rpm1.PNG" alt="linux_rpm1"><br></div>

<div align="center"><br><img src="/images/linux_rpm2.PNG" alt="linux_rpm2"><br></div>

<h5 id="yum"><a href="#yum" class="headerlink" title="yum"></a>yum</h5><p>为了解决rpm包的层层安装，解决依赖性问题，推荐使用<code>yum</code>直接安装</p>
<p>yum源：<br><code>vim /etc/yum.repos.d/CentOS-Base.repo</code>：打开默认的配置文件</p>
<div align="center"><br><img src="/images/linux_yum.PNG" alt="linux_yum"><br></div>

<p>常用的yum命令：<br><code>yum list</code>：查询所有可用的软件包列表<br><code>yum search [包名]</code>：搜索服务器上所有和关键字相关的包<br><code>yum -y install [包名]</code>：安装软件包<br><code>-y</code>：自动回答yes</p>
<p>升级软件包（如果不加包名会升级所有软件包，包括linux内核，会导致服务器崩溃，慎用）：<br><code>yum -y update [包名]</code></p>
<p>卸载软件包（和升级一样，小心使用，一定要写包名；慎用，卸载可能删掉某些公用包导致其它软件无法正常使用）：<br><code>yum -y remove [包名]</code></p>
<p><code>yum grouplist</code>：列出所有可用的软件组<br><code>yum groupinstall [软件组名]</code>：安装软件组<br><code>yum groupremove [软件组名]</code>：卸载指定软件组</p>
<h5 id="安装源码包"><a href="#安装源码包" class="headerlink" title="安装源码包"></a>安装源码包</h5><p>源码包保存位置：<code>/usr/local/src</code><br>软件安装位置：<code>/usr/local/</code></p>
<p>查看这个文件夹占用多大空间<br><code>du -sh [目录名]</code></p>
<p>安装源码包步骤（以安装apache为例）：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">准备：下载源码包，解压，进入解压目录（INSTALL文件有安装步骤和提示）</div><div class="line">命令：</div><div class="line">./configure --prefix=/usr/local/apache2（定义安装目录）</div><div class="line">作用：</div><div class="line">1.定义需要的功能选项</div><div class="line">2.检测环境是否符合安装需求</div><div class="line">3.把定义好的功能选项和信息写入Makefile文件，用于后续的编译</div></pre></td></tr></table></figure></p>
<p>然后执行<code>make</code>命令编译（前提linux已安装<code>gcc</code>，如果make失败可以用<code>make clean</code>来清除编译产生的临时数据）<br>最后<code>make install</code>编译安装</p>
<p>源码包的卸载直接删除安装后的文件夹即可，例如直接<code>rm -rf /usr/local/apache2</code>就可以完成卸载<br>有的软件包有自动安装脚本，直接运行<code>./setup.sh</code>即可完成安装</p>
<p>如果是下载的已经编译好的二进制文件，只需要解压后即可使用。例如java环境等，但为了方便操作，一般需要配置环境变量或者生成软连接到<code>/usr/local/bin/</code>目录下</p>
<h3 id="启动一个服务"><a href="#启动一个服务" class="headerlink" title="启动一个服务"></a>启动一个服务</h3><p>语法：<code>[绝对路径] start</code><br>例如<code>/etc/rc.d/init.d/httpd start</code>，这种启动方式适用于自己手动安装的软件包</p>
<p>另一种启动方式：<code>service httpd start</code>，适用于用rpm包安装的软件和yum安装的软件</p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h3&gt;&lt;p&gt;主要学习各种软件包的管理：源码包，二进制包，rpm包。&lt;/p&gt;
&lt;h3 id=&quot;软件包管理&quot;&gt;&lt;a href=&quot;#软件包管理&quot; class=&quot;headerlink&quot; title=&quot;软件包管理&quot;&gt;&lt;/a&gt;软件包管理&lt;/h3&gt;&lt;p&gt;软件包分为：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;源代码包&lt;/li&gt;
&lt;li&gt;二进制包（RPM包、系统默认包）&lt;/li&gt;
&lt;/ol&gt;
&lt;h5 id=&quot;rpm&quot;&gt;&lt;a href=&quot;#rpm&quot; class=&quot;headerlink&quot; title=&quot;rpm&quot;&gt;&lt;/a&gt;rpm&lt;/h5&gt;&lt;p&gt;RPM包的依赖性查询：&lt;code&gt;www.rpmfind.net&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;手动安装rpm包的最大坏处：&lt;br&gt;一个包的依赖太多，a-&amp;gt;b-&amp;gt;c树形依赖，或者a-&amp;gt;b-&amp;gt;c-&amp;gt;a环形依赖，使得安装rpm包变得很复杂&lt;/p&gt;
&lt;p&gt;RPM安装：&lt;br&gt;语法：&lt;code&gt;rpm -ivh [包全名]&lt;/code&gt;，参数：&lt;br&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;3&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;4&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;-i（install）：安装&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;-v（verbose）：显示详细信息&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;-h（hash）：显示进度&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;--nodeps：不检测依赖性&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="linux" scheme="http://zhangchaozc.cn/categories/linux/"/>
    
    
  </entry>
  
  <entry>
    <title>linux基础3-权限管理</title>
    <link href="http://zhangchaozc.cn/2018/02/12/linux/linux%E5%9F%BA%E7%A1%803-%E6%9D%83%E9%99%90%E7%AE%A1%E7%90%86/"/>
    <id>http://zhangchaozc.cn/2018/02/12/linux/linux基础3-权限管理/</id>
    <published>2018-02-12T14:33:27.000Z</published>
    <updated>2018-02-13T05:37:14.644Z</updated>
    
    <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>介绍一下linux中关于权限的操作。在文件管理中谈到，linux权限人分为：所有者，所属组和其他人，对文件的操作分为<code>r</code>读权限，<code>w</code>写权限和<code>x</code>执行权限，这节细说权限和讲如何更改权限。<br>截一张图细说<code>rwx</code>区别：</p>
<div align="center"><br><img src="/images/linux_rwx.PNG" alt="linux_rwx"><br></div>

<p>注意：删除一个文件不需要对此文件有写权限，只需要有对该文件所在的目录有写权限即可。<br><a id="more"></a></p>
<h3 id="权限管理"><a href="#权限管理" class="headerlink" title="权限管理"></a>权限管理</h3><h5 id="chmod"><a href="#chmod" class="headerlink" title="chmod"></a>chmod</h5><p><code>chmod [{ugoa}{+-=}{rwx}] [文件或目录]</code>：改变文件或目录权限<br>-R 递归修改<br>举例：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">//所有者加上执行权限</div><div class="line">chmod u+x xxx.txt</div><div class="line">//所有组加上写权限，其他人减去读权限</div><div class="line">chmod g+w,o-r xxx.txt</div><div class="line">//所有组的权限等于rwx</div><div class="line">chmod g=rwx xxx.txt</div></pre></td></tr></table></figure></p>
<p>另一种数字的表示方法：<br>r —- 4<br>w —- 2<br>x —- 1</p>
<p>相互转化：<br>532 =&gt; r-x-wx-w-<br>5代表u权限<br>3代表g权限<br>2代表o权限</p>
<h5 id="useradd"><a href="#useradd" class="headerlink" title="useradd"></a>useradd</h5><p><code>useradd xxx</code>：创建用户<br><code>passwd xxx</code>：更改用户密码<br><code>groupadd xxx</code>：创建用户组</p>
<h5 id="chown"><a href="#chown" class="headerlink" title="chown"></a>chown</h5><p><code>chown [用户] [文件或目录]</code>：改变文件的所有者，只有管理员root才能执行。<br><code>chgrp [用户组] [文件或目录]</code>：改变文件的所属组</p>
<h5 id="umask"><a href="#umask" class="headerlink" title="umask"></a>umask</h5><p>如何更改创建文件或文件夹时的默认权限？<br><code>umask -S</code>：查看默认权限<br>注意：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">直接umask显示0022，实际权限计算：首字母0不算，777-022=755就是实际表示的权限</div></pre></td></tr></table></figure></p>
<p><code>umask 023</code>：更改默认权限为777-023=754</p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h3&gt;&lt;p&gt;介绍一下linux中关于权限的操作。在文件管理中谈到，linux权限人分为：所有者，所属组和其他人，对文件的操作分为&lt;code&gt;r&lt;/code&gt;读权限，&lt;code&gt;w&lt;/code&gt;写权限和&lt;code&gt;x&lt;/code&gt;执行权限，这节细说权限和讲如何更改权限。&lt;br&gt;截一张图细说&lt;code&gt;rwx&lt;/code&gt;区别：&lt;/p&gt;
&lt;div align=&quot;center&quot;&gt;&lt;br&gt;&lt;img src=&quot;/images/linux_rwx.PNG&quot; alt=&quot;linux_rwx&quot;&gt;&lt;br&gt;&lt;/div&gt;

&lt;p&gt;注意：删除一个文件不需要对此文件有写权限，只需要有对该文件所在的目录有写权限即可。&lt;br&gt;
    
    </summary>
    
      <category term="linux" scheme="http://zhangchaozc.cn/categories/linux/"/>
    
    
  </entry>
  
  <entry>
    <title>linux基础2-文件编辑</title>
    <link href="http://zhangchaozc.cn/2018/02/12/linux/linux%E5%9F%BA%E7%A1%802-%E6%96%87%E4%BB%B6%E7%BC%96%E8%BE%91/"/>
    <id>http://zhangchaozc.cn/2018/02/12/linux/linux基础2-文件编辑/</id>
    <published>2018-02-12T14:19:00.000Z</published>
    <updated>2018-02-13T06:03:18.687Z</updated>
    
    <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>文件查看、浏览、编辑的命令有很多，这里就当做个笔记了。</p>
<h3 id="文件浏览或编辑"><a href="#文件浏览或编辑" class="headerlink" title="文件浏览或编辑"></a>文件浏览或编辑</h3><h5 id="touch"><a href="#touch" class="headerlink" title="touch"></a>touch</h5><p>创建一个文件，关于带空格的文件名，不建议创建。<br>如果文件名必须带空格，可以用：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">touch &quot;pro files&quot;</div></pre></td></tr></table></figure></p>
<p>用双引号包含。所有命令中对带空格的文件名，都必须用双引号括住。</p>
<h5 id="cat"><a href="#cat" class="headerlink" title="cat"></a>cat</h5><p><code>cat [文件名]</code>：查看文件<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">cat -n：查看行号</div></pre></td></tr></table></figure></p>
<h5 id="tac"><a href="#tac" class="headerlink" title="tac"></a>tac</h5><p><code>tac [文件名]</code>：倒着查看文件</p>
<a id="more"></a>
<h5 id="more"><a href="#more" class="headerlink" title="more"></a>more</h5><p>如何浏览大文件？<br><code>more [文件名]</code>：浏览大文件（分页）<br>技巧：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">空格，f：翻页查看（下一页）</div><div class="line">回车：一行一行查看</div><div class="line">q：退出浏览</div></pre></td></tr></table></figure></p>
<h5 id="less"><a href="#less" class="headerlink" title="less"></a>less</h5><p>相比more，如何搜索，向上翻页？<br><code>less [文件名]</code>：分页查看大文件<br>在more命令的基础上：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">PgUp向上翻页</div><div class="line">/搜索关键字，按键n，跳至下一个关键字</div></pre></td></tr></table></figure></p>
<h5 id="head"><a href="#head" class="headerlink" title="head"></a>head</h5><p>只想看前几行？<br><code>head -n xx [文件名]</code><br>默认显示前10行</p>
<p>反之，想看后几行？<br><code>tail -n xx [文件名]</code><br>默认10行<br><code>tail -f</code>可以动态查看。</p>
<h3 id="链接的创建"><a href="#链接的创建" class="headerlink" title="链接的创建"></a>链接的创建</h3><p>软链接：<code>ln -s [原文件] [链接文件]</code><br>硬链接：<code>ln [原文件] [链接文件]</code><br>软链接相当于原文件的快捷方式。权限都是rwx。<br>硬链接相当于原文件的拷贝，但是这两个文件可以同步更新，且这两个文件的i节点相同。硬链接不能跨分区生成。</p>
<h3 id="vim"><a href="#vim" class="headerlink" title="vim"></a>vim</h3><p>强大的文本编辑器<br>语法：<code>vim [文件名]</code></p>
<p>编辑模式：<code>i，a，o，I，A，O</code>，退出编辑模式按<code>Esc</code></p>
<div align="center"><br><img src="/images/linux_vim1.PNG" alt="linux_vim1"><br></div>

<p>命令模式：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">:set nu 设置行号</div><div class="line">:set nonu 取消行号</div><div class="line">gg 第一行</div><div class="line">G 最后一行</div><div class="line">nG 到第n行</div><div class="line">:n 到第n行</div><div class="line">$ 移至行尾</div><div class="line">0 移至行首</div></pre></td></tr></table></figure></p>
<p>复制剪切操作：</p>
<div align="center"><br><img src="/images/linux_vim2.PNG" alt="linux_vim2"><br></div>

<p>删除操作：</p>
<div align="center"><br><img src="/images/linux_vim3.PNG" alt="linux_vim3"><br></div>

<p>替换字符和取消操作：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">r：替换光标所在的字符</div><div class="line">R：从光标处开始替换字符，按[Esc]结束</div><div class="line">u：取消上一步的操作</div></pre></td></tr></table></figure></p>
<p>搜索和less，more一样：按/来搜索，n切换下一个结果，搜索时忽略大小写：<code>:set ic</code></p>
<p>替换字符串：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">:%s/oldstr/newstr/g：全文替换</div><div class="line">:n1,n2s/oldstr/newstr/g：在指定范围内替换</div></pre></td></tr></table></figure></p>
<p>最后退出vim：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">:w 保存修改</div><div class="line">:w new_file 另存为指定文件</div><div class="line">:wq 保存修改并退出</div><div class="line">:q! 不保存修改退出</div><div class="line">ZZ 快捷键退出，相当于wq了</div></pre></td></tr></table></figure></p>
<p>高级vim操作，技巧<br><code>:r [文件名]</code>：导入其它文件到当前光标处<br><code>:![命令名]</code>：在不退出vim的情况下执行命令<br><code>:r ![命令名]</code>：将执行命令的结果导入当前光标处<br><code>:n1,n2s/^/#/g</code>：连续注释行<br><code>:n1,n2s/^#/ /g</code>：把连续注释的行去掉<br><code>:ab mymail xxxx@abc.com</code>：替换，这样的话，在vim编辑中，只要输入mymail后，都会被替换成指定值</p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h3&gt;&lt;p&gt;文件查看、浏览、编辑的命令有很多，这里就当做个笔记了。&lt;/p&gt;
&lt;h3 id=&quot;文件浏览或编辑&quot;&gt;&lt;a href=&quot;#文件浏览或编辑&quot; class=&quot;headerlink&quot; title=&quot;文件浏览或编辑&quot;&gt;&lt;/a&gt;文件浏览或编辑&lt;/h3&gt;&lt;h5 id=&quot;touch&quot;&gt;&lt;a href=&quot;#touch&quot; class=&quot;headerlink&quot; title=&quot;touch&quot;&gt;&lt;/a&gt;touch&lt;/h5&gt;&lt;p&gt;创建一个文件，关于带空格的文件名，不建议创建。&lt;br&gt;如果文件名必须带空格，可以用：&lt;br&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;touch &amp;quot;pro files&amp;quot;&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
&lt;p&gt;用双引号包含。所有命令中对带空格的文件名，都必须用双引号括住。&lt;/p&gt;
&lt;h5 id=&quot;cat&quot;&gt;&lt;a href=&quot;#cat&quot; class=&quot;headerlink&quot; title=&quot;cat&quot;&gt;&lt;/a&gt;cat&lt;/h5&gt;&lt;p&gt;&lt;code&gt;cat [文件名]&lt;/code&gt;：查看文件&lt;br&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;cat -n：查看行号&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
&lt;h5 id=&quot;tac&quot;&gt;&lt;a href=&quot;#tac&quot; class=&quot;headerlink&quot; title=&quot;tac&quot;&gt;&lt;/a&gt;tac&lt;/h5&gt;&lt;p&gt;&lt;code&gt;tac [文件名]&lt;/code&gt;：倒着查看文件&lt;/p&gt;
    
    </summary>
    
      <category term="linux" scheme="http://zhangchaozc.cn/categories/linux/"/>
    
    
  </entry>
  
  <entry>
    <title>linux基础1-文件管理</title>
    <link href="http://zhangchaozc.cn/2018/02/12/linux/linux%E5%9F%BA%E7%A1%801-%E6%96%87%E4%BB%B6%E7%AE%A1%E7%90%86/"/>
    <id>http://zhangchaozc.cn/2018/02/12/linux/linux基础1-文件管理/</id>
    <published>2018-02-12T13:21:39.000Z</published>
    <updated>2018-02-13T05:25:52.724Z</updated>
    
    <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>之前linux基础太差，干啥都要百度，很不方便，所以决定系统化的学习一下，恶补恶补。这几个系列也算是我的学习笔记了。</p>
<h3 id="基础常识"><a href="#基础常识" class="headerlink" title="基础常识"></a>基础常识</h3><p>windows文件系统格式有：FAT16，FAT32，NTFS；linux有：EXT2，EXT3，EXT4。<br>Linux不区分文件扩展名，有的特殊文件（压缩包）有扩展名是为了管理员区分方便。这个跟windows不一样。</p>
<h3 id="运营者需要了解的"><a href="#运营者需要了解的" class="headerlink" title="运营者需要了解的"></a>运营者需要了解的</h3><ol>
<li>远程服务器不允许关机，只能重启。（某种情况远程关了，打开麻烦）</li>
<li>重启时应该关闭所有服务。</li>
<li>不在访问高峰期执行高负载命令。</li>
<li>远程配置防火墙时不要把自己踢出服务器。</li>
<li>指定合理的密码规范并定期更新。</li>
<li>合理分配权限。</li>
<li>定期备份重要数据和日志。</li>
</ol>
<a id="more"></a>
<h3 id="Linux各个目录的含义"><a href="#Linux各个目录的含义" class="headerlink" title="Linux各个目录的含义"></a>Linux各个目录的含义</h3><p>这里截了几张图片：</p>
<div align="center"><br><img src="/images/linux_dir1.PNG" alt="linux_dir1"><br></div>

<div align="center"><br><img src="/images/linux_dir2.PNG" alt="linux_dir2"><br></div>

<div align="center"><br><img src="/images/linux_dir3.PNG" alt="linux_dir3"><br></div>

<h3 id="命令规则"><a href="#命令规则" class="headerlink" title="命令规则"></a>命令规则</h3><p>命令格式：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">命令 [-选项] [参数]</div><div class="line">举例： ls -la /etc</div></pre></td></tr></table></figure></p>
<p>简化选项与完整选项：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">-a 等于 --all</div><div class="line">简化选项：- 加 简化字母</div><div class="line">完整选项：-- 加 完整字母</div></pre></td></tr></table></figure></p>
<h3 id="基础的文件管理命令"><a href="#基础的文件管理命令" class="headerlink" title="基础的文件管理命令"></a>基础的文件管理命令</h3><h5 id="ls"><a href="#ls" class="headerlink" title="ls"></a>ls</h5><p>作用：列出所有的文件和目录。<br>常用选项：<br>-a 显示所有文件，包括隐藏文件<br>-l 显示详细信息<br>-d 查看目录属性（只查看这个目录，而不查看目录下的文件信息）<br>-h 更友好的显示，例如 ls -lh，文件大小会帮你换算为K<br>-i 查看文件的节点序号（相当于id号，linux中任何文件或目录都有唯一标识的id号，便于系统管理）</p>
<p>举例：<br>输入<code>ls -l</code>，结果：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">-rw-rw-r--. 1 zhangchao zhangchao   22 Feb  6 20:17 hh</div></pre></td></tr></table></figure></p>
<p>依次表示的信息是：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">- 开头代表一个文件，d 开头代表是一个目录，l开头代表一个软链接</div><div class="line">rw-rw-r--. 代表权限信息，3个一拆分，[rw-]、[rw-]和[r--]，分别是所有者，所属组，其他人的权限</div><div class="line">1 代表调用次数</div><div class="line">后面两个用户代表：所有者，所有组</div><div class="line">文件大小</div><div class="line">最后一次修改时间/创建时间</div><div class="line">文件名</div></pre></td></tr></table></figure></p>
<p>关于linux的权限的小知识：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line">u 所有者</div><div class="line">g 所有组</div><div class="line">o 其他人</div><div class="line">r 读</div><div class="line">w 写</div><div class="line">x 执行</div><div class="line"></div><div class="line">-rw-rw-r--. 的意思是</div><div class="line">-：这是一个文件</div><div class="line">rw-：它的所有者有读写权限</div><div class="line">rw-：它的所有组有读写权限</div><div class="line">r--：其他人有读权限</div></pre></td></tr></table></figure></p>
<h5 id="mkdir"><a href="#mkdir" class="headerlink" title="mkdir"></a>mkdir</h5><p>作用：创建一个目录<br>如果我要创建/a/b/c目录，本身a目录是不存在的，即如何递归创建？<br><code>mkdir -p</code>即可递归创建。<br>创建目录可以同时创建多个目录，用空格隔开。</p>
<p>有关的小命令：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">cd 切换到某个目录（基础之基础）</div><div class="line">pwd 显示当前目录的完整路径（现在cd的是哪个目录？）</div><div class="line">rmdir 删除空目录（只能删除空目录）</div></pre></td></tr></table></figure></p>
<h5 id="cp"><a href="#cp" class="headerlink" title="cp"></a>cp</h5><p>作用：原意copy，即文件的复制。可以复制多个文件，复制的同时可以给文件重命名。<br>用法：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">cp [原文件] [新的位置]</div><div class="line">cp -r 复制目录</div><div class="line">cp -p 保留原文件的属性</div></pre></td></tr></table></figure></p>
<h5 id="mv"><a href="#mv" class="headerlink" title="mv"></a>mv</h5><p>作用：移动/剪切文件或目录（不需要加<code>-r</code>），文件的重命名。<br>用法：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">mv [原文件] [新的位置]</div><div class="line">mv [原文件] [新名字]</div></pre></td></tr></table></figure></p>
<h5 id="rm"><a href="#rm" class="headerlink" title="rm"></a>rm</h5><p>作用：删除一个文件、目录。<br>用法：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">rm [文件]</div><div class="line">rm -r [目录]</div><div class="line">-f 强制执行</div><div class="line">rm -rf [目录] 递归的删除目录和目录里面的所有</div></pre></td></tr></table></figure></p>
<p>如果一不小心<code>rm -rf /</code>，就gg了。</p>
<h5 id="find"><a href="#find" class="headerlink" title="find"></a>find</h5><p>文件搜索命令（windows下文件搜索工具everything）<br>基本语法：<code>find [搜索范围] [匹配条件]</code><br><code>find /home -name xxx</code>：根据名字搜索<br>如何模糊搜索？利用通配符，*匹配任意字符，?匹配单个字符，举例：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">find /home -name *xxx*  （查找多个值需要&quot;*xxx*&quot;，双引号引起来）</div></pre></td></tr></table></figure></p>
<p><code>find /home -iname xxx</code>：不区分大小写搜索<br>如何按文件大小来筛选？（+代表大于，-代表小于，否则等于），举例：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">find /home -size +204800  （查找大于100MB的数据，注意数据格式为数据块，1数据块=512B=0.5KB）</div></pre></td></tr></table></figure></p>
<p><code>find /home -user zhangchao</code>：按用户来查找，同理用户组为<code>-group</code></p>
<h6 id="高级查找"><a href="#高级查找" class="headerlink" title="高级查找"></a>高级查找</h6><p><code>find /home -cmin -5</code>：在home目录下查找5分钟内被修改过属性的文件和目录，参数：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">-amin 访问时间</div><div class="line">-cmin 文件属性</div><div class="line">-mmin 文件内容</div></pre></td></tr></table></figure></p>
<p><code>find /home -size +163840 -a -size -204800</code>：查找文件大于80MB小于100MB的文件，参数：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">-a 表示两个条件同时满足，记作and</div><div class="line">-o 表示满足任意一个条件即可，记作or</div></pre></td></tr></table></figure></p>
<p>如何根据类型查找？<br><code>-type</code>根据文件类型查找，其中：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">f：文件</div><div class="line">d：目录</div><div class="line">l：软链接</div></pre></td></tr></table></figure></p>
<p>如何对查找出来的结果做进一步的操作？<br><code>find /home -name xxx -exec ls -l {} \;</code>：对查找出来的结果进行批量的ls -l操作，其中后面的<code>[-exec ls -l {} \;]</code>是一个固定的格式<code>-exec</code>可以用<code>-ok</code>代替，只是每个文件都要询问一下。<br>根据i节点查找？举例：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">找到当前目录下，i节点为12345的文件并删掉它</div><div class="line">find . -inum 12345 -exec rm &#123;&#125; \;</div></pre></td></tr></table></figure></p>
<h5 id="locate"><a href="#locate" class="headerlink" title="locate"></a>locate</h5><p><code>find</code>命令需要遍历查找文件夹，浪费大量资源，如何高效查找？<br><code>locate</code>命令在文件库中查找文件，不在硬盘中查找，所以速度很快，语法<code>locate [文件名]</code></p>
<p>如果一个文件新建不久，资料库中没有，就无法找到<br>可以使用命令<code>updatedb</code>更新资料库，<code>不会</code>更新系统的<code>/tmp</code>文件夹中的内容</p>
<p><code>locate -i [文件名]</code>：不区分大小写的搜寻</p>
<h5 id="which"><a href="#which" class="headerlink" title="which"></a>which</h5><p>特殊查找，查找命令在哪个文件夹。<br>语法：<code>which [命令名]</code><br>例如：<code>which ls</code><br>除了显示命令的地址所在，还可以显示命令的别名。例如普通的<code>ls</code>可以显示文件、文件夹的颜色，其实<code>ls</code>是<code>ls --color</code>的别名，如果要执行底层的<code>ls</code>，可以用<code>\ls</code>。</p>
<h5 id="whereis"><a href="#whereis" class="headerlink" title="whereis"></a>whereis</h5><p>语法：<code>whereis [命令名]</code><br>和<code>which</code>功能一样，<code>whereis</code>还可以查到该命令的帮助文档所在地址。</p>
<h5 id="grep"><a href="#grep" class="headerlink" title="grep"></a>grep</h5><p>查找某个确定文件中是否有某某关键字。<br>语法：<code>grep [指定匹配字符串] [文件名]</code>，参数：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">-i：不区分大小写</div><div class="line">-v：排除指定字符串</div></pre></td></tr></table></figure></p>
<p>例：<br><code>grep -v ^# /xxx.txt</code>：排除#开头的内容</p>
<h5 id="man"><a href="#man" class="headerlink" title="man"></a>man</h5><p>查看指定命令的帮助手册<br>语法：<code>man [命令名]</code><br>例如：<code>man ls</code><br>如果直接查看配置文件信息，可以<code>man [配置文件名]</code><br>例如：<code>man services</code></p>
<blockquote>
<p>小知识，帮助文档的<code>文件名</code>中含有<code>1</code>代表命令的帮助，<code>5</code>代表配置文件的帮助</p>
</blockquote>
<p>其他查看帮助信息的命令：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">whatis [命令名]</div><div class="line">apropos [配置文件名]</div></pre></td></tr></table></figure></p>
<p>还有命令加上 –help，查看帮助<br>例如<code>ls --help</code></p>
<p>注意：内置shell命令（例如<code>cd</code>）无法<code>man</code>，<code>which</code>等，这时查看帮助可以用<code>help [命令名]</code><br>例如：<code>help cd</code></p>
<h3 id="压缩-amp-解压命令"><a href="#压缩-amp-解压命令" class="headerlink" title="压缩&amp;解压命令"></a>压缩&amp;解压命令</h3><h5 id="gzip"><a href="#gzip" class="headerlink" title="gzip"></a>gzip</h5><p>压缩成<code>.gz</code>格式的压缩包<br>语法：<code>gzip [文件名]</code>，生成<code>.gz</code>压缩文件<br>解压缩：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">gunzip [压缩文件]</div><div class="line">gzip -d [压缩文件]</div></pre></td></tr></table></figure></p>
<p>这种方法压缩，解压后，<code>不保留原文件</code>；<code>只能压缩文件，不能压缩目录</code>。</p>
<h5 id="tar"><a href="#tar" class="headerlink" title="tar"></a>tar</h5><p>另一种方法打包压缩<br>语法：<code>tar [选项] [压缩后文件名] [目录]</code>，参数：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">-c 打包</div><div class="line">-v 显示详细信息</div><div class="line">-f 指定文件名</div><div class="line">-z 打包的同时压缩/解压缩</div><div class="line">-x 解压缩</div></pre></td></tr></table></figure></p>
<p>例如<br><code>tar -cvf subtemp.tar subtemp</code>：仅打包<br><code>tar -cvzf subtemp.tar.gz subtemp</code>：打包并压缩，这个是最常见的压缩包<code>.tar.gz</code><br>解压时候把<code>-c</code>换成<code>-x</code>即可</p>
<p>注意<code>f</code>字母要写在最后，不能<code>-zcfv</code>等，<code>z</code>跟gz压缩格式有关</p>
<h5 id="zip"><a href="#zip" class="headerlink" title="zip"></a>zip</h5><p>语法：<code>zip [选项] [压缩后文件名] [文件或目录]</code>，参数：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">-r：压缩目录</div></pre></td></tr></table></figure></p>
<p>解压：<code>unzip [压缩文件名]</code></p>
<h5 id="bzip2"><a href="#bzip2" class="headerlink" title="bzip2"></a>bzip2</h5><p>gzip的升级版，bzip2<br>语法：<code>bzip2 [选项] [文件]</code>，参数：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">-k：产生压缩文件后保留原文件</div></pre></td></tr></table></figure></p>
<p>跟tar一起使用<br><code>tar -cvjf subtemp.tar.bz2 subtemp</code>：打包并压缩</p>
<p>把<code>z</code>换成<code>j</code>压缩成<code>.bz2</code>格式的压缩包，解压同理<code>-c</code>换成<code>-x</code></p>
<p>解压缩：<br>语法：<code>bunzip2 [压缩文件名]</code></p>
<p>总结一下（文件后缀只是方便人区分，linux系统不认后缀的）：</p>
<ol>
<li><code>.gz</code>是<code>gzip</code>命令压缩产生的，这个只能压缩单个文件</li>
<li><code>.tar</code>是<code>tar</code>命令产生的，可以打包多个文件，文件夹</li>
<li><code>.zip</code>是<code>zip</code>命令，其余同<code>tar</code></li>
<li><code>.bz2</code>是<code>gzip</code>的升级版，除了能加<code>-k</code>参数保留原文件以外，其它没功能加强</li>
</ol>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h3&gt;&lt;p&gt;之前linux基础太差，干啥都要百度，很不方便，所以决定系统化的学习一下，恶补恶补。这几个系列也算是我的学习笔记了。&lt;/p&gt;
&lt;h3 id=&quot;基础常识&quot;&gt;&lt;a href=&quot;#基础常识&quot; class=&quot;headerlink&quot; title=&quot;基础常识&quot;&gt;&lt;/a&gt;基础常识&lt;/h3&gt;&lt;p&gt;windows文件系统格式有：FAT16，FAT32，NTFS；linux有：EXT2，EXT3，EXT4。&lt;br&gt;Linux不区分文件扩展名，有的特殊文件（压缩包）有扩展名是为了管理员区分方便。这个跟windows不一样。&lt;/p&gt;
&lt;h3 id=&quot;运营者需要了解的&quot;&gt;&lt;a href=&quot;#运营者需要了解的&quot; class=&quot;headerlink&quot; title=&quot;运营者需要了解的&quot;&gt;&lt;/a&gt;运营者需要了解的&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;远程服务器不允许关机，只能重启。（某种情况远程关了，打开麻烦）&lt;/li&gt;
&lt;li&gt;重启时应该关闭所有服务。&lt;/li&gt;
&lt;li&gt;不在访问高峰期执行高负载命令。&lt;/li&gt;
&lt;li&gt;远程配置防火墙时不要把自己踢出服务器。&lt;/li&gt;
&lt;li&gt;指定合理的密码规范并定期更新。&lt;/li&gt;
&lt;li&gt;合理分配权限。&lt;/li&gt;
&lt;li&gt;定期备份重要数据和日志。&lt;/li&gt;
&lt;/ol&gt;
    
    </summary>
    
      <category term="linux" scheme="http://zhangchaozc.cn/categories/linux/"/>
    
    
  </entry>
  
  <entry>
    <title>spring回顾</title>
    <link href="http://zhangchaozc.cn/2018/01/01/spring/spring%E5%9B%9E%E9%A1%BE/"/>
    <id>http://zhangchaozc.cn/2018/01/01/spring/spring回顾/</id>
    <published>2018-01-01T14:54:54.000Z</published>
    <updated>2018-01-01T14:58:24.453Z</updated>
    
    <content type="html"><![CDATA[<h3 id="IOC"><a href="#IOC" class="headerlink" title="IOC"></a>IOC</h3><p>控制反转，脱离在代码中new对象，用什么对象去xml中配置，去取。</p>
<h3 id="DI"><a href="#DI" class="headerlink" title="DI"></a>DI</h3><p>依赖注入，IOC的一种实现方式</p>
<h3 id="scope"><a href="#scope" class="headerlink" title="scope"></a>scope</h3><p>单例、多例</p>
<h3 id="Bean生命周期"><a href="#Bean生命周期" class="headerlink" title="Bean生命周期"></a>Bean生命周期</h3><p>init-method=”” 初始化<br>destroy-method=”” 销毁前的操作<br>//全局的配置<br>default-init-method=””<br>default-destroy-method=””</p>
<a id="more"></a>
<h3 id="注解"><a href="#注解" class="headerlink" title="注解"></a>注解</h3><p>@Component = 在xml中写一个bean，默认id为类名首字母小写，可以自己改id：@Component(“xxx_id”)<br>    @Repository = 用于dao层<br>    @Service = 用于service层<br>    @Controller = 用于web层<br>@Autowired = 提供set方法注入<br>    对于默认的spring bean例如ApplicationContext，可以直接注解使用<br>    牛皮的它也能注解List&lt;&gt;、Map&lt;&gt;等集合<br>    和@Resource的区别：其实也没啥区别，@Autowired byType注入，后者byName注入。<br>@Scope = 配置单例、多例<br>@Qualifier = 一个接口有多个实现类，如下代码，你想让它实现哪个。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="meta">@Autowired</span></div><div class="line"><span class="meta">@Qualifier</span>(<span class="string">"beanImplTwo"</span>)</div><div class="line"><span class="keyword">private</span> BeanInterface beanInterface;</div></pre></td></tr></table></figure></p>
<h3 id="把配置文件当类写（用的比较少）"><a href="#把配置文件当类写（用的比较少）" class="headerlink" title="把配置文件当类写（用的比较少）"></a>把配置文件当类写（用的比较少）</h3><p>@Configuration、@Bean等</p>
<div align="center"><br><img src="/images/spring_config.PNG" alt="spring_config"><br></div>

<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div></pre></td><td class="code"><pre><div class="line"><span class="meta">@Configuration</span></div><div class="line"><span class="meta">@ImportResource</span>(<span class="string">"classpath:config.xml"</span>)</div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">StoreConfig</span> </span>&#123;</div><div class="line"></div><div class="line">    <span class="meta">@Value</span>(<span class="string">"$&#123;url&#125;"</span>)</div><div class="line">    <span class="keyword">private</span> String url;</div><div class="line"></div><div class="line">    <span class="meta">@Value</span>(<span class="string">"$&#123;jdbc.username&#125;"</span>)</div><div class="line">    <span class="keyword">private</span> String username;</div><div class="line"></div><div class="line">    <span class="meta">@Value</span>(<span class="string">"$&#123;password&#125;"</span>)</div><div class="line">    <span class="keyword">private</span> String password;</div><div class="line"></div><div class="line">    <span class="meta">@Bean</span></div><div class="line">    <span class="function"><span class="keyword">public</span> MyDriverManager <span class="title">myDriverManager</span><span class="params">()</span> </span>&#123;</div><div class="line">        <span class="keyword">return</span> <span class="keyword">new</span> MyDriverManager(url, username, password);</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="meta">@Bean</span>(name = <span class="string">"stringStore"</span>, initMethod = <span class="string">"init"</span>, destroyMethod = <span class="string">"destroy"</span>)</div><div class="line">    <span class="function"><span class="keyword">public</span> Store <span class="title">stringStore</span><span class="params">()</span> </span>&#123;</div><div class="line">        <span class="keyword">return</span> <span class="keyword">new</span> StringStore();</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="meta">@Bean</span>(name = <span class="string">"stringStore"</span>)</div><div class="line">    <span class="meta">@Scope</span>(value = <span class="string">"prototype"</span>, proxyMode = ScopedProxyMode.TARGET_CLASS)</div><div class="line">    <span class="function"><span class="keyword">public</span> Store <span class="title">stringStore</span><span class="params">()</span> </span>&#123;</div><div class="line">        <span class="keyword">return</span> <span class="keyword">new</span> StringStore();</div><div class="line">    &#125;</div><div class="line"></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="AOP"><a href="#AOP" class="headerlink" title="AOP"></a>AOP</h3><p>面向切面编程：日志记录，性能统计，安全控制，事务管理……。</p>
<div align="center"><br><img src="/images/aop.PNG" alt="aop"><br></div>

<p>实现方式：</p>
<ol>
<li>预编译。</li>
<li>动态代理。</li>
</ol>
<p>常见：注解，不改变原代码的情况下，为原代码增加新的功能。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;IOC&quot;&gt;&lt;a href=&quot;#IOC&quot; class=&quot;headerlink&quot; title=&quot;IOC&quot;&gt;&lt;/a&gt;IOC&lt;/h3&gt;&lt;p&gt;控制反转，脱离在代码中new对象，用什么对象去xml中配置，去取。&lt;/p&gt;
&lt;h3 id=&quot;DI&quot;&gt;&lt;a href=&quot;#DI&quot; class=&quot;headerlink&quot; title=&quot;DI&quot;&gt;&lt;/a&gt;DI&lt;/h3&gt;&lt;p&gt;依赖注入，IOC的一种实现方式&lt;/p&gt;
&lt;h3 id=&quot;scope&quot;&gt;&lt;a href=&quot;#scope&quot; class=&quot;headerlink&quot; title=&quot;scope&quot;&gt;&lt;/a&gt;scope&lt;/h3&gt;&lt;p&gt;单例、多例&lt;/p&gt;
&lt;h3 id=&quot;Bean生命周期&quot;&gt;&lt;a href=&quot;#Bean生命周期&quot; class=&quot;headerlink&quot; title=&quot;Bean生命周期&quot;&gt;&lt;/a&gt;Bean生命周期&lt;/h3&gt;&lt;p&gt;init-method=”” 初始化&lt;br&gt;destroy-method=”” 销毁前的操作&lt;br&gt;//全局的配置&lt;br&gt;default-init-method=””&lt;br&gt;default-destroy-method=””&lt;/p&gt;
    
    </summary>
    
      <category term="spring" scheme="http://zhangchaozc.cn/categories/spring/"/>
    
    
  </entry>
  
  <entry>
    <title>webxml中filter配置报红线</title>
    <link href="http://zhangchaozc.cn/2018/01/01/maven/webxml%E4%B8%ADfilter%E9%85%8D%E7%BD%AE%E6%8A%A5%E7%BA%A2%E7%BA%BF/"/>
    <id>http://zhangchaozc.cn/2018/01/01/maven/webxml中filter配置报红线/</id>
    <published>2018-01-01T14:48:11.000Z</published>
    <updated>2018-01-01T14:53:05.060Z</updated>
    
    <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>用maven新建一个web项目后，配置web.xml，很常见的一段配置（编码配置）：<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">filter</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>CharacterEncodingFilter<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">filter-class</span>&gt;</span>org.springframework.web.filter.CharacterEncodingFilter<span class="tag">&lt;/<span class="name">filter-class</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">init-param</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">param-name</span>&gt;</span>encoding<span class="tag">&lt;/<span class="name">param-name</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">param-value</span>&gt;</span>utf-8<span class="tag">&lt;/<span class="name">param-value</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">init-param</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">filter</span>&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">filter-mapping</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>CharacterEncodingFilter<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/*<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">filter-mapping</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p>如图：</p>
<div align="center"><br><img src="/images/webxml.png" alt="webxml"><br></div>

<p>居然报红线，这是要逼死强迫症。</p>
<h3 id="解决"><a href="#解决" class="headerlink" title="解决"></a>解决</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">&lt;web-app xmlns:xsi=&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</div><div class="line">         xmlns=&quot;http://java.sun.com/xml/ns/javaee&quot;</div><div class="line">         xsi:schemaLocation=&quot;http://java.sun.com/xml/ns/javaee http://java.sun.com/xml/ns/javaee/web-app_3_0.xsd&quot;</div><div class="line">         version=&quot;3.0&quot;&gt;</div><div class="line">&lt;/web-app&gt;</div></pre></td></tr></table></figure>
<p>这样即可。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h3&gt;&lt;p&gt;用maven新建一个web项目后，配置web.xml，很常见的一段配置（编码配置）：&lt;br&gt;&lt;figure class=&quot;highlight
    
    </summary>
    
      <category term="maven" scheme="http://zhangchaozc.cn/categories/maven/"/>
    
    
  </entry>
  
  <entry>
    <title>自己搭建VPN服务器</title>
    <link href="http://zhangchaozc.cn/2017/11/19/vpn/%E8%87%AA%E5%B7%B1%E6%90%AD%E5%BB%BAVPN%E6%9C%8D%E5%8A%A1%E5%99%A8/"/>
    <id>http://zhangchaozc.cn/2017/11/19/vpn/自己搭建VPN服务器/</id>
    <published>2017-11-19T13:40:25.000Z</published>
    <updated>2017-11-19T14:46:03.430Z</updated>
    
    <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>最近网络管理很严，以前在<a href="http://www.see-world.club/index.php/Index/seeyou.html" target="_blank" rel="external">see world</a>上可以买节点，但是现在那个倒闭了，有时候想访问谷歌查查资料就很不方便，看了一篇教程后，就自己搭建了一个<code>VPN服务器</code>，特来分享（注：翻墙是为了学习用，不可用于商业用途如售卖节点等）。</p>
<h3 id="shadowsocks"><a href="#shadowsocks" class="headerlink" title="shadowsocks"></a>shadowsocks</h3><p>这个分为客户端和服务器端，以前服务端是购买的别人的节点，现在需要自己搭建服务，所以需要一个国外的服务器。<br>客户端自己<a href="https://github.com/shadowsocks/shadowsocks-windows/releases" target="_blank" rel="external">下载</a>就可以。</p>
<h3 id="购买服务器"><a href="#购买服务器" class="headerlink" title="购买服务器"></a>购买服务器</h3><p>学长推荐，我是在<a href="http://banwagong.cn/gonglue.html" target="_blank" rel="external">搬瓦工</a>上购买的：</p>
<div align="center"><br><img src="/images/banwagong1.PNG" alt="banwagong1"><br></div>

<p>可以看到，最便宜的只要1.99美元/月，可是这个售空了，我买的是4.99的那个。</p>
<p>购买后，IP和密码都会发到你的邮箱，默认的用户名是root（用支付宝就可以直接购买，很方便）。</p>
<a id="more"></a>
<h3 id="搭建"><a href="#搭建" class="headerlink" title="搭建"></a>搭建</h3><p>用SSH工具登录服务器，这里推荐一个一键安装的脚本，命令行：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">wget --no-check-certificate https://raw.githubusercontent.com/teddysun/shadowsocks_install/master/shadowsocks.sh</div></pre></td></tr></table></figure></p>
<p>如果提示没有<code>wget</code>指令，就先<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">yum install wget</div></pre></td></tr></table></figure></p>
<p>一下，有了脚本，安装也是无脑的，两行命令：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">chmod +x shadowsocks.sh</div><div class="line">./shadowsocks.sh 2&gt;&amp;1 | tee shadowsocks.log</div></pre></td></tr></table></figure></p>
<p>输入完后，会提示让你设置密码，输好回车后会让你选择一个端口，输入 1-65535 间的数字都行。然后是设置加密方式，输入某个加密方式的序号就行。</p>
<div align="center"><br><img src="/images/vpn1.jpg" alt="vpn1"><br></div>

<p>慢慢等待，安装完后会把配置信息都打印出来，记住自己的配置。现在基本就可以了，用配置的信息在客户端配置就行。</p>
<div align="center"><br><img src="/images/vpn2.png" alt="vpn2"><br></div>

<h3 id="设置多用户"><a href="#设置多用户" class="headerlink" title="设置多用户"></a>设置多用户</h3><p>如果要设置多用户，直接编辑配置文件：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">nano /etc/shadowsocks.json</div></pre></td></tr></table></figure></p>
<p>这个命令是编辑文件，可以直接修改json文件：<br><figure class="highlight json"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">    <span class="attr">"server"</span>:<span class="string">"0.0.0.0"</span>,</div><div class="line">    <span class="attr">"server_port"</span>:<span class="number">13162</span>,</div><div class="line">    <span class="attr">"local_address"</span>:<span class="string">"127.0.0.1"</span>,</div><div class="line">    <span class="attr">"local_port"</span>:<span class="number">1080</span>,</div><div class="line">    <span class="attr">"port_password"</span>:&#123;</div><div class="line">        <span class="attr">"xxx1"</span>:<span class="string">"xxx1"</span>,</div><div class="line">        <span class="attr">"xxx2"</span>:<span class="string">"xxx2"</span></div><div class="line">    &#125;,</div><div class="line">    <span class="attr">"timeout"</span>:<span class="number">300</span>,</div><div class="line">    <span class="attr">"method"</span>:<span class="string">"rc4-md5"</span>,</div><div class="line">    <span class="attr">"fast_open"</span>:<span class="literal">true</span></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p><code>port_password</code>字段就是端口-密码的意思，可以自己添加（ctrl+x 退出编辑，按提示保存文件）。</p>
<p>添加完后重启服务：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">/etc/init.d/shadowsocks restart</div></pre></td></tr></table></figure></p>
<p>即可生效。</p>
<h3 id="开启高速服务"><a href="#开启高速服务" class="headerlink" title="开启高速服务"></a>开启高速服务</h3><p>改三个文件：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">nano /etc/rc.local</div></pre></td></tr></table></figure></p>
<p>末尾加上：<code>echo 3 &gt; /proc/sys/net/ipv4/tcp_fastopen</code>。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">nano /etc/sysctl.conf</div></pre></td></tr></table></figure>
<p>末尾加上：<code>net.ipv4.tcp_fastopen = 3</code>。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">nano /etc/shadowsocks.json</div></pre></td></tr></table></figure>
<p>打开配置文件，设置”fast_open”:true。</p>
<p>最后重启服务即可。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h3&gt;&lt;p&gt;最近网络管理很严，以前在&lt;a href=&quot;http://www.see-world.club/index.php/Index/seeyou.html&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;see world&lt;/a&gt;上可以买节点，但是现在那个倒闭了，有时候想访问谷歌查查资料就很不方便，看了一篇教程后，就自己搭建了一个&lt;code&gt;VPN服务器&lt;/code&gt;，特来分享（注：翻墙是为了学习用，不可用于商业用途如售卖节点等）。&lt;/p&gt;
&lt;h3 id=&quot;shadowsocks&quot;&gt;&lt;a href=&quot;#shadowsocks&quot; class=&quot;headerlink&quot; title=&quot;shadowsocks&quot;&gt;&lt;/a&gt;shadowsocks&lt;/h3&gt;&lt;p&gt;这个分为客户端和服务器端，以前服务端是购买的别人的节点，现在需要自己搭建服务，所以需要一个国外的服务器。&lt;br&gt;客户端自己&lt;a href=&quot;https://github.com/shadowsocks/shadowsocks-windows/releases&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;下载&lt;/a&gt;就可以。&lt;/p&gt;
&lt;h3 id=&quot;购买服务器&quot;&gt;&lt;a href=&quot;#购买服务器&quot; class=&quot;headerlink&quot; title=&quot;购买服务器&quot;&gt;&lt;/a&gt;购买服务器&lt;/h3&gt;&lt;p&gt;学长推荐，我是在&lt;a href=&quot;http://banwagong.cn/gonglue.html&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;搬瓦工&lt;/a&gt;上购买的：&lt;/p&gt;
&lt;div align=&quot;center&quot;&gt;&lt;br&gt;&lt;img src=&quot;/images/banwagong1.PNG&quot; alt=&quot;banwagong1&quot;&gt;&lt;br&gt;&lt;/div&gt;

&lt;p&gt;可以看到，最便宜的只要1.99美元/月，可是这个售空了，我买的是4.99的那个。&lt;/p&gt;
&lt;p&gt;购买后，IP和密码都会发到你的邮箱，默认的用户名是root（用支付宝就可以直接购买，很方便）。&lt;/p&gt;
    
    </summary>
    
      <category term="vpn" scheme="http://zhangchaozc.cn/categories/vpn/"/>
    
    
      <category term="翻墙教程" scheme="http://zhangchaozc.cn/tags/%E7%BF%BB%E5%A2%99%E6%95%99%E7%A8%8B/"/>
    
  </entry>
  
  <entry>
    <title>xmldb踩坑</title>
    <link href="http://zhangchaozc.cn/2017/11/04/%E6%95%B0%E6%8D%AE%E5%BA%93/xmldb%E8%B8%A9%E5%9D%91/"/>
    <id>http://zhangchaozc.cn/2017/11/04/数据库/xmldb踩坑/</id>
    <published>2017-11-04T03:21:58.000Z</published>
    <updated>2017-11-04T03:25:38.340Z</updated>
    
    <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>这是一个困扰我几天的bug，必须写篇博客纪念一下。最近给导师做一个OA管理系统，数据库采用的是<code>oracle xmldb</code>数据库，呵呵，之前没用过这个，这个数据库的存储是基于xml的，又不能使用数据库框架，于是开始了踩坑之旅。</p>
<h3 id="数据库方面"><a href="#数据库方面" class="headerlink" title="数据库方面"></a>数据库方面</h3><p>基于xsd和xml，xsd是用来规范xml的，就相当于xsd就是表的抽象吧，而xml就是存放的数据。这个没啥坑，直接贴代码了。</p>
<h5 id="注册xsd"><a href="#注册xsd" class="headerlink" title="注册xsd"></a>注册xsd</h5><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">create or replace directory xmldir as &apos;xxxx&apos;;  //你放xsd的文件夹</div></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line">begin</div><div class="line">dbms_xmlschema.registerSchema(</div><div class="line">	schemaurl=&gt;&apos;xxx.xsd&apos;,  //xxx.xsd是你的要注册的文件</div><div class="line">	schemadoc=&gt;bfilename(&apos;XMLDIR&apos;,&apos;xxx.xsd&apos;),</div><div class="line">	local=&gt;TRUE,</div><div class="line">	genbean=&gt;FALSE,</div><div class="line">	gentables=&gt;FALSE,</div><div class="line">	force=&gt;FALSE,</div><div class="line">	owner=&gt;&apos;xx&apos;  //当前登录的用户名</div><div class="line">);</div><div class="line">end;</div><div class="line">/</div></pre></td></tr></table></figure>
<a id="more"></a>
<h5 id="根据xsd建表"><a href="#根据xsd建表" class="headerlink" title="根据xsd建表"></a>根据xsd建表</h5><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">create table xxx of xmltype xmlschema &quot;xxx.xsd&quot; element &quot;xxxxx&quot;;</div></pre></td></tr></table></figure>
<h5 id="删除xsd"><a href="#删除xsd" class="headerlink" title="删除xsd"></a>删除xsd</h5><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">begin</div><div class="line">dbms_xmlschema.deleteSchema(</div><div class="line">	schemaurl=&gt;&apos;xxx.xsd&apos;,</div><div class="line">	DELETE_OPTION=&gt;dbms_xmlschema.DELETE_CASCADE_FORCE</div><div class="line">);</div><div class="line">end;</div><div class="line">/</div></pre></td></tr></table></figure>
<h3 id="java客户端"><a href="#java客户端" class="headerlink" title="java客户端"></a>java客户端</h3><p>前面都不算坑，用java操作数据库就问题来了，除了连接数据库的包外，还需要两个包：<code>xdb.jar</code>,<code>xmlparserv2</code>。</p>
<p>因为在maven仓库里面实在找不到这个，需要自己导入，问题来了，当时我给xdb.jar的groupId取名为<code>oracle.xdb</code>，会报错如下：</p>
<div align="center"><br><img src="/images/xdb.png" alt="xdb"><br></div>

<p><code>java.lang.NullPointerException</code>我就呵呵了，问题出在<code>getObject函数</code>，当时也搞了好久，最后发现groupId和artifactId改成xdb就好了，所以，不要给自己新添加的包乱取名字！</p>
<h3 id="spring调试"><a href="#spring调试" class="headerlink" title="spring调试"></a>spring调试</h3><p>用spring的测试框架比较方便，可是问题又来了，一运行起来就报错：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line">Caused by: oracle.xml.parser.schema.XSDException: Duplicated definition for: &apos;identifiedType&apos;</div><div class="line">	at oracle.xml.parser.schema.XSDBuilder.buildSchema(XSDBuilder.java:921)</div><div class="line">	at oracle.xml.parser.schema.XSDBuilder.build(XSDBuilder.java:558)</div><div class="line">	at oracle.xml.parser.schema.XSDValidator.processSchemaLocation(XSDValidator.java:1024)</div><div class="line">	at oracle.xml.parser.schema.XSDValidator.startElement(XSDValidator.java:629)</div><div class="line">	at oracle.xml.parser.v2.NonValidatingParser.parseElement(NonValidatingParser.java:1320)</div><div class="line">	at oracle.xml.parser.v2.NonValidatingParser.parseRootElement(NonValidatingParser.java:364)</div><div class="line">	at oracle.xml.parser.v2.NonValidatingParser.parseDocument(NonValidatingParser.java:310)</div><div class="line">	... 39 more</div></pre></td></tr></table></figure></p>
<p>在网上找了很久的解决办法，最后发现了是<code>xmlparserv2</code>的错误，删除jar包里面的<code>\META-INF\services</code>文件夹，就好了。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h3&gt;&lt;p&gt;这是一个困扰我几天的bug，必须写篇博客纪念一下。最近给导师做一个OA管理系统，数据库采用的是&lt;code&gt;oracle xmldb&lt;/code&gt;数据库，呵呵，之前没用过这个，这个数据库的存储是基于xml的，又不能使用数据库框架，于是开始了踩坑之旅。&lt;/p&gt;
&lt;h3 id=&quot;数据库方面&quot;&gt;&lt;a href=&quot;#数据库方面&quot; class=&quot;headerlink&quot; title=&quot;数据库方面&quot;&gt;&lt;/a&gt;数据库方面&lt;/h3&gt;&lt;p&gt;基于xsd和xml，xsd是用来规范xml的，就相当于xsd就是表的抽象吧，而xml就是存放的数据。这个没啥坑，直接贴代码了。&lt;/p&gt;
&lt;h5 id=&quot;注册xsd&quot;&gt;&lt;a href=&quot;#注册xsd&quot; class=&quot;headerlink&quot; title=&quot;注册xsd&quot;&gt;&lt;/a&gt;注册xsd&lt;/h5&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;create or replace directory xmldir as &amp;apos;xxxx&amp;apos;;  //你放xsd的文件夹&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;3&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;4&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;5&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;6&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;7&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;8&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;9&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;10&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;11&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;12&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;begin&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;dbms_xmlschema.registerSchema(&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;	schemaurl=&amp;gt;&amp;apos;xxx.xsd&amp;apos;,  //xxx.xsd是你的要注册的文件&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;	schemadoc=&amp;gt;bfilename(&amp;apos;XMLDIR&amp;apos;,&amp;apos;xxx.xsd&amp;apos;),&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;	local=&amp;gt;TRUE,&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;	genbean=&amp;gt;FALSE,&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;	gentables=&amp;gt;FALSE,&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;	force=&amp;gt;FALSE,&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;	owner=&amp;gt;&amp;apos;xx&amp;apos;  //当前登录的用户名&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;);&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;end;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;/&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
    
    </summary>
    
      <category term="数据库" scheme="http://zhangchaozc.cn/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    
    
      <category term="bug" scheme="http://zhangchaozc.cn/tags/bug/"/>
    
      <category term="oracle" scheme="http://zhangchaozc.cn/tags/oracle/"/>
    
      <category term="XmlDb" scheme="http://zhangchaozc.cn/tags/XmlDb/"/>
    
  </entry>
  
  <entry>
    <title>修改原有jar的配置</title>
    <link href="http://zhangchaozc.cn/2017/10/25/java/%E4%BF%AE%E6%94%B9%E5%8E%9F%E6%9C%89jar%E7%9A%84%E9%85%8D%E7%BD%AE/"/>
    <id>http://zhangchaozc.cn/2017/10/25/java/修改原有jar的配置/</id>
    <published>2017-10-25T13:59:50.000Z</published>
    <updated>2017-10-25T14:06:08.281Z</updated>
    
    <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>之前用到一个jar包，但是里面的配置写死了，这个工具不适合自己用，但是如何修改配置使得可以拿来用呢？</p>
<h3 id="解压"><a href="#解压" class="headerlink" title="解压"></a>解压</h3><p>jar文件本身也是一个压缩文件，直接解压后，可以看到原来的文件，一般都是编译后的.class文件，如果想自己加入代码的话，可以通过反编译工具拿到源码，修改后再重新打包成jar。<code>IntelliJ IDEA</code>这个编辑器就直接可以反编译。</p>
<p>暂时不用修改代码，只改了配置文件。</p>
<h3 id="打包"><a href="#打包" class="headerlink" title="打包"></a>打包</h3><p>直接命令：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">jar cvfm xxx.jar ./META-INF/MANIFEST.MF ./</div></pre></td></tr></table></figure></p>
<p>需要注意的是，<code>/META-INF/MANIFEST.MF</code>这个文件很重要，一般若干个.class打包成jar时，都要指定主类和其它的配置，MANIFEST.MF文件就是干这个的。</p>
<p>原来的这个文件不要动，<code>./</code>是对当前目录的所有文件进行打包。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h3&gt;&lt;p&gt;之前用到一个jar包，但是里面的配置写死了，这个工具不适合自己用，但是如何修改配置使得可以拿来用呢？&lt;/p&gt;
&lt;h3 id=&quot;解压&quot;&gt;&lt;a 
    
    </summary>
    
      <category term="Java" scheme="http://zhangchaozc.cn/categories/Java/"/>
    
    
  </entry>
  
</feed>
